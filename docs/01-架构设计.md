# 合同审批审查系统 — 架构设计

版本：v2.0
日期：2026-09-14
状态：**生效中** —— 本文档是阶段划分与职责边界的唯一基准

> **v2.0 变更说明**：项目架构已从"单体 Backend 承担全部职责"调整为
> **Frontend → AI Agent Service → Backend Service → MySQL** 的分层结构，
> 全部智能能力（解析 / 条款 / 元数据 / 关键词 / 规则求值 / LLM / 编排）归属 **Agent**，
> Backend 退为**数据层 + 存储层 + 基础能力层**。
>
> ⚠️ **P12 修正**：**报告导出不在此列**。审查报告由 **Backend** 基于已持久化的
> ReviewTask 结果**实时生成 Markdown** —— 不重跑 LLM / 规则 / 合并 / 解析，
> 不建 `report` 表。见 §17.3 的 P12 行。
>
> 受影响章节已同步更新：**§1.1 职责边界**、**§2 三端目录**、**§3 依赖关系**、**§4 数据流**、
> **§8 API 分类**、**§9 / §10 归属说明**、**§15 MVP 范围**、**§17 阶段划分**。
> 旧路线（旧 P5 ~ P11）**已作废**，见 §17.0。

---

## 0. 环境现状与技术选型决策

### 0.1 已探明的环境事实

| 项 | 实测结果 |
|---|---|
| 工作目录 | `E:\python\contract_approval` |
| 当前项目内容 | **空壳**：仅 `.idea/`、`.venv/`、`pyproject.toml`（dependencies 为空数组），无任何业务代码 |
| **Python（项目 venv）** | **3.12.8** —— `.venv/` 由 uv 创建，`home = D:\IDE\python312`。**这才是项目实际使用的解释器** |
| Python（系统 PATH） | 3.13.9 —— 仅存在于系统 PATH，**与项目 venv 无关**，不要据此判断运行时版本 |
| 依赖管理 | **uv 0.11.25**；venv 内**没有 pip**，所有依赖操作走 `uv add` / `uv sync`；`uv.lock` 为版本锁定文件（必须提交） |
| Node / npm | Node v24.14.0，npm 11.9.0 |
| MySQL | 8.4.8，本地 `D:\IDE\MySQL\bin\mysql.exe` |
| 版本控制 | ✅ 已在 P2-a 完成 `git init`（默认分支 main）+ `.gitignore` |

结论：**不存在需要兼容的历史代码，一切从零开始**。

> 📌 勘误：本节初版把"系统 PATH 的 Python 3.13.9"误记为项目 Python 版本，实际项目 venv 是 3.12.8。
> 该勘误已在 P2-a 阶段核实并修正（此项曾影响 §0.2 第 4 项的选型理由，理由已同步修正，**选型结论未变**）。

### 0.2 技术选型确认表

需求指定：Python + FastAPI + SQLAlchemy + MySQL + Vue3 + Vite + Element Plus。以下全部采纳，另有 6 处需要调整或明确，理由逐条说明：

| # | 项 | 结论 | 理由 |
|---|---|---|---|
| 1 | Web 框架 | FastAPI ✅ 采纳 | 原生 async、Pydantic 强校验、自动 OpenAPI，与「前后端数据结构必须一致」的要求天然契合 |
| 2 | ORM | SQLAlchemy 2.x（async + `asyncmy`/`aiomysql`） | 2.x 的 `Mapped[]` 类型注解让模型即文档；异步驱动避免解析/审查时长事务阻塞连接池 |
| 3 | 数据库 | MySQL 8.4 ✅ 采纳 | 本地已装；需要 `utf8mb4_0900_ai_ci` 与 JSON 列（规则表达式、LLM 原始响应） |
| 4 | **OCR 引擎** | **RapidOCR (onnxruntime)** ⚠️ 调整 | PaddleOCR 依赖完整的 `paddlepaddle` 运行时：体积大、Windows 安装链路长、模型加载慢，而 OCR 在本项目是**可选链路**（仅扫描件走）。RapidOCR 就是 PaddleOCR 的中文模型导出成 ONNX 后纯 onnxruntime 推理：依赖轻、Windows 原生可跑、中文合同识别效果同级。**接口层做 `OCREngine` 抽象，随时可换回 PaddleOCR**。<br>（原表述"因 Python 3.13 下 paddlepaddle wheel 覆盖不全"基于误记的系统 Python 版本，项目 venv 实为 **3.12.8**，该理由不成立，已于 P2-a 修正；**选型结论不变**。） |
| 5 | **异步任务** | **DB 队列 + 独立 worker 进程** ⚠️ 调整 | Celery 在 Windows 需要 `--pool=solo`/gevent workaround，且本地演示链路要额外运维 Redis。改用 MySQL 表做队列（`SELECT ... FOR UPDATE SKIP LOCKED`），零额外中间件，且**阻塞/重试/幂等/断点续跑**四个语义完全可控。抽象出 `TaskDispatcher` 接口，将来换 Celery/Redis 只改一个适配器实现 |
| 6 | PDF 解析 | PyMuPDF（fitz）⚠️ **许可标注** | 抽取文本 + 渲染页面 + 字符级坐标一次搞定，性能最好。**但它是 AGPL-3.0**：企业内网自用可接受，对外分发有传染性。文档明确标注该风险；若需许可干净，替换为 `pypdfium2`(Apache/BSD) + `pdfplumber`(MIT)，适配层已隔离 |
| 7 | **报告导出** | **Markdown，Backend 实时生成（无模板引擎、零新依赖）** ⚠️ **P12 定案** | 报告是**已持久化审查结果**的只读投影，标准库字符串拼接即可，不引 Jinja2 / Playwright / reportlab。中文零风险（不涉及字体嵌入与分页），且与 §12 审批回写要用的 Markdown 同源。PDF 不在 P12 范围内 |
| 8 | DOCX 解析 | `python-docx`（含表格） | 唯一成熟选择；**注意它不提供页码**，页码问题见 §10.4 |
| 9 | 前端文档预览 | PDF → `pdfjs-dist`；DOCX → `docx-preview` | 两者都能拿到 DOM/文本层，是「原文高亮双向锚定」的前提，且不需要服务端预转换 |
| 10 | LLM 接入 | 统一 `LLMProvider` 抽象，首版实现 `DeepSeekProvider` + `AnthropicProvider` | 见 §9.1。DeepSeek 走 OpenAI 兼容协议；Anthropic 走**官方 `anthropic` SDK**（不得用 OpenAI 兼容 shim 冒充 Claude） |
| 11 | 前端状态/请求 | Pinia + Axios（统一拦截器 + 错误码映射） | Element Plus 生态标配 |
| 12 | 迁移 | Alembic | 表结构会反复演进，必须有版本化迁移，禁止 `create_all` 上线 |

> ⚠️ 以上 4 处「调整」我已按**推荐方案**写进本设计。若你坚持原方案（例如必须用 Celery 或 PaddleOCR），告诉我，我只需要改 §2 目录下对应的适配器实现，不影响其它设计。

> 📌 **v2.0 补充：选型的「归属」变了，选型结论本身没变。**
>
> | 行 | 归属 | 说明 |
> |---|---|---|
> | 4 OCR（RapidOCR） | **AI Agent** | 能力本身归 Agent，选型不变 |
> | 5 异步任务（DB 队列） | **Backend**，但**后置** | 选型不变（仍不用 Celery/Redis）。当前 Agent 在 HTTP 请求内同步跑完 Graph，队列与 Worker 属 §17.4 的 P14 |
> | 6 PDF 解析（PyMuPDF） | **AI Agent** | 同上；**黄金链路只做 DOCX**，PDF 属扩展阶段 |
> | 8 DOCX 解析（python-docx） | **AI Agent** | 同上 |
> | 10 LLM 接入 | **AI Agent** | Provider 抽象归 Agent，见 §9 归属说明 |
> | 2 ORM / 3 DB / 12 迁移 | **Backend** | 只有 Backend 碰数据库 —— Agent 侧不存在这些依赖 |

---

## 1. 完整系统架构

### 1.1 分层架构图与三端职责边界

```
┌──────────────────────────────────────────────────────────────────────┐
│  浏览器  Vue3 + Vite + Element Plus + Pinia + Vue Router             │
│  ┌────────────┬────────────┬────────────┬────────────┐               │
│  │ 审查大盘    │ 智能审查    │ 规则与模板  │ Mock审批    │               │
│  │ Dashboard  │ 工作台      │ 配置页      │ 系统页      │               │
│  └────────────┴────────────┴────────────┴────────────┘               │
│   通用组件：DocumentViewer / RiskCard / HighlightAnchor / StatusTag     │
│   ★ 只做展示与交互：调 API、渲染结果、人工审核操作。不做任何业务判断      │
└──────────┬────────────────────────────────────┬──────────────────────┘
           │ 上传合同 / 发起审查                  │ 查询结果 / 人工审核
           │ POST /api/agent/review              │ /api/v1/**
           ▼                                     │
┌──────────────────────────────────────────────────┼───────────────────┐
│  AI Agent Service   FastAPI + LangGraph           │  127.0.0.1:8001  │
│  ★ 智能审查流程的唯一归属地                                            │
│                                                                      │
│  LangGraph Workflow（编排骨架）                                       │
│   START → upload_file → validate_file ─[条件边]→ parse_document       │
│        → identify_clauses → extract_metadata → extract_keywords       │
│        → rule_review → llm_review → merge_risks                       │
│        → generate_suggestions → persist_result → END                  │
│                                                                      │
│  State │ Node │ Edge │ Conditional Edge │ Tool                        │
│  能力域：parsers/ │ understanding/ │ rules/ │ llm/                    │
│  ⚠️ 不引入 SQLAlchemy / aiomysql —— Agent 不直接访问数据库              │
└───────────────────────────┬──────────────────────────────────────────┘
                            │ httpx → Backend 领域 API
                            │ （Agent 唯一的数据出入口）
                            ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Backend Service   FastAPI + SQLAlchemy 2.x      127.0.0.1:8000      │
│  ★ 数据层 + 存储层 + 基础能力层                                        │
│                                                                      │
│  接入层  /api/v1/**  contracts │ tasks │ documents │ risks │ ...      │
│  服务层  ContractIngest │ ReviewTask │ HumanReview │ ...              │
│  存储    StorageBackend（LocalStorageBackend）                        │
│  幂等    文件级 contract_file.sha256 │ 任务级 review_task.idempotency_key│
│                                                                      │
│  ⚠️ 不含 LangGraph / Prompt / LLM / 条款与风险的智能判断               │
└───────────────────────────┬──────────────────────────────────────────┘
┌───────────────────────────▼──────────────────────────────────────────┐
│  数据访问层  db/  SQLAlchemy 2.x Models + Repository + Alembic 迁移     │
└───────────────────────────┬──────────────────────────────────────────┘
┌───────────────────────────▼──────────────────────────────────────────┐
│  MySQL 8.4 (utf8mb4)  │  backend/storage/（文件）  │  LLM API (DeepSeek) │
└──────────────────────────────────────────────────────────────────────┘
```

#### 三端职责边界（架构裁决，不可越界）

> **这是本项目最重要的一条约束。** 它决定了"某个功能该写在哪个服务里"这类问题的唯一答案。
> 判断方法只有一个：**这个能力是"智能判断"还是"数据与基础能力"？** 前者归 Agent，后者归 Backend。

| 端 | 负责 | **明确不做** |
|---|---|---|
| **Frontend**<br>Vue3 | 页面展示 · 用户交互 · 调用 Agent / Backend API · 展示审查进度与结果 · 人工审核操作 | 文档解析 · Clause Identification · Metadata / Keyword Extraction · 风险判断 · Prompt · LLM 调用 · LangGraph 工作流 |
| **AI Agent**<br>FastAPI + LangGraph | LangGraph workflow · State / Node / Edge / Conditional Edge · 文档解析 · Clause Identification · Metadata Extraction · Keyword Extraction · Rule Review · LLM Review · Risk Merge · Suggestion · Report · Prompt · LLM 调用 · Agent Tools | **直连数据库**（不得依赖 SQLAlchemy / aiomysql / MySQL）· 已有 Backend 能力的重复实现（文件校验 / SHA256 / 幂等 / 存储） |
| **Backend**<br>FastAPI + SQLAlchemy | Contract · ContractFile · ReviewTask 基础数据 · 文件存储 · 文件上传 · SHA256 · 文件级幂等 · 任务级幂等 · 数据库访问 · 基础 CRUD API · 审查结果持久化 · Human Review API · Approval / Comment 等基础业务数据 | LangGraph · Prompt · LLM · Agent orchestration · Clause / Metadata / Keyword 智能提取 · 风险智能判断 |

**Agent 与 Backend 的通信关系**

```
Agent  ──httpx──►  Backend 领域 API（/api/v1/**）
       ◄──JSON───

* 方向唯一：Agent 是客户端，Backend 是服务端。Backend 从不反向调用 Agent。
* 通道唯一：HTTP。Agent 侧不出现 SQLAlchemy / 数据库连接串 / Alembic。
* 契约唯一：两侧的字段以 Backend 的 Pydantic Schema 为准，Agent 侧的 DTO 与之对齐。
* 职责复用的正确姿势：Agent 需要"上传文件"时，**调 Backend 的接口**，而不是自己再写一遍
  校验 / 哈希 / 幂等 —— 否则会出现两套会各自漂移的规则（见 §3 的反面例子）。
```

**Human Review 的边界（重要）**

人工审核**不进入 LangGraph**，也不使用 `interrupt()` / checkpoint / resume 实现：

```
自动流程（Agent）:  ... → generate_suggestions → persist_result → END

之后（普通 Backend 业务）:
        Frontend ──► Backend Human Review API ──► 人工确认 / 驳回 / 修改
```

理由：人工审核的等待时间以小时/天计，把它做进图里意味着要长期持有可恢复的执行上下文与 checkpoint 存储；而它本质上是**普通的状态流转**，用一张表的 `review_status` 字段就能表达。
两个状态维度不要混为一谈：

| 字段 | 归属 | 取值 | 用途 |
|---|---|---|---|
| `review_task.status` | Backend | pending / parsing / reviewing / blocked / completed | Agent / ReviewTask 的**自动流程**状态 |
| `risk_item.review_status` | Backend | PENDING / CONFIRMED / REJECTED / MODIFIED | **单个风险项**的人工复核状态 |

### 1.2 关键架构原则（贯穿全项目，也是面试可讲点）

| 原则 | 落地方式 |
|---|---|
| **智能归 Agent，数据归 Backend** | 见 §1.1 三端职责边界。判断新功能归属只有一个问题："这是智能判断，还是数据与基础能力？" |
| **Agent 不碰数据库** | Agent 的 `pyproject.toml` 刻意不含 SQLAlchemy / aiomysql；数据读写一律经 Backend 领域 API。**约束靠依赖声明保证，不靠自觉** |
| **不重复实现已归属于对方的逻辑** | Agent 需要上传文件就调 Backend 的上传接口，不自己再写一遍校验/哈希/幂等。两套实现必然漂移 |
| **单一状态机入口** | 所有任务状态变更只能通过 `ReviewTaskService.transition(to_status, reason)`，内部校验合法性 + 写 `task_event`。禁止任何地方直接 `task.status = xxx` |
| **能力域可替换** | 解析器、OCR、LLM、审批客户端全部面向抽象接口编程，实现类通过配置注入（工厂 + settings 开关） |
| **统一坐标系** | 一切定位（风险、建议、批注、元数据）都落在同一个「归一化文本流全局字符偏移」坐标系上，见 §10.3 |
| **LLM 只出结构化数据** | Provider 层强制 schema 校验，输出 Pydantic 对象；前端永远不解析自然语言 |
| **LLM 不许自报坐标** | LLM 只允许 `quote` 原文片段，`char_start/char_end` 由 **Agent** 在原文中反查回填，杜绝幻觉偏移 |
| **幂等优先** | 任务执行、审批回写全部带幂等键（报告导出是**纯只读**，无副作用因而不需要幂等键） |
| **可观测** | 结构化日志 + `task_event` 时间线 + `ai_call_log` 每次模型调用全量留痕 |
| **锁不跨长任务** | DB 行锁只覆盖"抢任务"这一次 UPDATE，OCR/LLM 全程在事务外执行（§1.4） |

### 1.3 并发模型：async / to_thread / 进程池 的边界（关键）

Worker 是单个 asyncio 事件循环。**任何同步阻塞调用都会卡死整个 event loop**，导致心跳停止、所有在跑任务被自愈机制误判为卡死。因此必须显式划分三类操作：

| 类别 | 具体操作 | 执行方式 | 理由 |
|---|---|---|---|
| **原生 async** | LLM HTTP 调用（httpx.AsyncClient / anthropic AsyncClient）、数据库读写（asyncmy + SQLAlchemy async）、附件下载 | `await` 直接跑在 event loop | I/O 密集，await 期间让出控制权，天然并发 |
| **同步阻塞 I/O** | `python-docx` 读文件、PyMuPDF 打开/抽取/渲染、本地文件读写、Playwright 调用 | `await asyncio.to_thread(...)` | 这些库是 C 扩展且无 async 版本，必须扔默认线程池（`min(32, cpu+4)`） |
| **CPU / Native 密集** | OCR 推理（RapidOCR/onnxruntime）、图像预处理（灰度/二值化/去噪）、大页渲染 | 默认独立 `ProcessPoolExecutor`（`OCR_MAX_WORKERS` 默认 **1**，可配 1/2/4），**懒加载**；可配置切回线程池 | OCR 属于秒级 CPU/Native 计算任务，独立进程可避免长时间计算与 Worker event loop 调度争用；**实际基准测试允许切换为线程池**（见下方 `OCR_EXECUTOR_MODE`） |

**7 条硬规则（写进代码规范，Code Review 检查项）：**

1. **OCR 默认走进程池**：`await loop.run_in_executor(ocr_pool, engine.recognize, img_path)`。OCR 属于秒级 CPU/Native 计算任务，独立进程可避免长时间计算与 Worker event loop 调度争用。
   - **懒加载（lazy init）**：worker 启动时**不**创建 OCR 进程池，首次真正遇到需要 OCR 的文件时才创建（`ensure_ocr_pool()` + 双检锁，保证只创建一次）。黄金链路只跑 DOCX，**P2 不会提前拉起一个永不使用的 OCR 进程池**。
   - **并发度受控**：`OCR_MAX_WORKERS` 默认 **1**，可配置 1 / 2 / 4。默认取 1 是为了避免 `Worker 数 × 进程池大小` 的进程爆炸（2 个 worker × 4 = 8 个常驻进程，且各自加载一份 OCR 模型）。
   - 每个子进程内各自初始化一份 OCR session，避免每页重复加载模型。
2. **`OCR_EXECUTOR_MODE` 可配置**：`process`（默认）/ `thread`。用进程池的理由是避免与 event loop 调度争用；**若实测（如 onnxruntime 已充分释放 GIL、或单核受限机器上进程调度开销更大）线程池表现更好，可切到 `thread`，无需改代码**。这个开关的存在本身就是"用基准测试说话、而非拍脑袋"的体现。
3. **解析库一律走默认线程池**：`await asyncio.to_thread(parser.parse, path)`。PyMuPDF 的 `fitz.open` 对象**不能跨线程共享**，必须保证"打开 → 解析 → 关闭"在同一个线程内完成。
4. **DB session 绝不进线程池**：SQLAlchemy `AsyncSession` 绑定 event loop，只允许在 event loop 内的协程中使用。解析函数若需落库，必须返回纯数据 DTO，回到 event loop 后再写。
5. **心跳与主流程解耦**：心跳由独立的 asyncio 后台任务每 30s 写一次，**不依赖主流程推进**。即使 OCR 在某页卡了 2 分钟，心跳仍在跳，不会被误判为卡死。
6. **并发上限可配**：同一时刻跑 `WORKER_CONCURRENCY`（默认 2）个任务，每个任务内部阶段串行；REVIEW 阶段的多批 LLM 调用用 `asyncio.Semaphore(4)` 限流并发。
7. **执行器降级**：若 `ProcessPoolExecutor` 创建失败（受限环境），自动降级为 `to_thread` 并记 warn 日志 —— 功能不中断，性能下降。

PARSE 阶段的实际调用形态（说明边界，非最终代码）：

```python
async def run_parse_stage(task_id: int, file: ContractFile) -> None:
    # 1. 同步阻塞 I/O → 默认线程池
    blocks_raw = await asyncio.to_thread(PARSER_REGISTRY[file.file_ext].parse, file.storage_path)

    if blocks_raw.needs_ocr:                        # 2. CPU 密集 → 独立进程池
        loop = asyncio.get_running_loop()
        blocks_raw = await loop.run_in_executor(ocr_pool, ocr_pages, blocks_raw.image_pages)

    blocks = TextNormalizer.normalize(blocks_raw)   # 3. 毫秒级纯内存计算 → 直接在 event loop 跑
    await save_blocks(blocks)                       # 4. async DB
    await run_metadata_and_clause_stage(...)        # 5. 内部 await LLM（async）
```

> 第 3 步的取舍值得说明：**不是所有 CPU 操作都要扔线程池**。判断标准是"单次耗时是否可感知（>10ms）且是否高频调用"。几万字符的归一化是毫秒级，扔线程池的调度开销反而更大；只有 OCR / 图像处理这种秒级 CPU 任务才需要进程池。这个判断本身就是面试可讲的点。

### 1.4 DB Queue 抢占的事务边界（关键）

**核心原则：锁只覆盖"抢任务"这一瞬间的 UPDATE，绝不跨越任何长耗时操作。**

```
┌──────────────────── 事务 T1（毫秒级，唯一持锁区间）────────────────────┐
│ BEGIN                                                                  │
│ SELECT id, contract_id, file_id, current_stage, retry_count            │
│   FROM review_task                                                     │
│  WHERE status = 'pending'                                              │
│    AND (next_retry_at IS NULL OR next_retry_at <= NOW(3))              │
│  ORDER BY priority DESC, id ASC                                        │
│  LIMIT 1                                                               │
│  FOR UPDATE SKIP LOCKED;   ← SKIP LOCKED：其它 worker 直接跳过被锁行    │
│                                                                        │
│ -- 未取到行 → COMMIT，sleep 后重试（空闲轮询，见下）                    │
│                                                                        │
│ UPDATE review_task                                                     │
│    SET status='parsing', worker_id=:wid, locked_at=NOW(3),             │
│        heartbeat_at=NOW(3), started_at=COALESCE(started_at,NOW(3)),    │
│        version=version+1                                               │
│  WHERE id=:id AND status='pending';   ← 双保险，rowcount 必须 = 1       │
│                                                                        │
│ INSERT INTO task_event(task_id, from_status, to_status, reason,        │
│                         operator)                                      │
│   VALUES (:id,'pending','parsing','worker_claimed',:wid);              │
│ COMMIT;   ← ★ 锁在此释放。此后 OCR / LLM 全部在事务外执行               │
└────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──── 事务外：长耗时阶段（可能数分钟，期间不持有任何数据库行锁）────┐
│ 1. 启动心跳后台任务（每 30s 一条独立短事务：                       │
│    UPDATE review_task SET heartbeat_at=NOW(3)                     │
│     WHERE id=:id AND worker_id=:wid  → 立即 COMMIT）               │
│ 2. PARSE：OCR/解析（进程池/线程池）→ 完成后用独立短事务分批写       │
│    document_block / contract_metadata / clause                    │
│ 3. 阶段切换：独立短事务 UPDATE status='reviewing' + 写 task_event  │
│ 4. REVIEW：LLM 批量调用 → 逐批用独立短事务写 risk_item             │
│ 5. 收尾：独立短事务 UPDATE status='completed', finished_at + 事件   │
│ 6. finally：取消心跳；异常交 ErrorClassifier 决定 blocked / 重试    │
└───────────────────────────────────────────────────────────────────┘
```

**6 条硬规则：**

1. **锁的范围 = 一条 UPDATE**。`FOR UPDATE` 事务内**禁止**出现任何 `await` 的 I/O（LLM/OCR/文件下载/解析），禁止解析文件。
2. **`SKIP LOCKED` 只用于抢 `pending` 任务**。不要用它扫 `parsing/reviewing` —— 那是自愈扫描的职责，用普通查询 + 超时条件（§13.5）。
3. **状态二次校验**：`UPDATE ... WHERE id=:id AND status='pending'`，`rowcount != 1` 说明被他人抢走或状态已变，**静默放弃**本次任务继续下一轮，不报错。
4. **心跳必须是独立短事务**：独立 `async_session()`，UPDATE 完立即 commit，绝不与主流程共用一个长 session。
5. **每次落库都是独立短事务**：5000 个 block 不能一个事务写完，需分批 commit（每 500 条一次）。这既是避免长事务锁等待/undo 膨胀，也是**断点续跑**的前提 —— 已写入的部分不因后续失败而回滚。
6. **幂等兜底**：worker 崩溃导致状态卡在 `parsing` 时，自愈机制（§13.5）把超过 5 分钟无心跳的任务回退 `pending`；重跑时按 `contract_id + current_stage` 检查产物是否已存在，**已完成的阶段直接跳过**。

**空闲轮询策略**：未抢到任务时 `sleep(1s)`；连续 10 次空转后退避到 `sleep(5s)`；一旦抢到任务立即恢复 1s。避免空队列时空转打满 CPU 与数据库。

---

## 2. 三端项目目录结构

> 图例：**【已实现】** 代码已存在并通过测试 · **【空目录】** 骨架已建、归属已变更 · **【待建】** 后续阶段创建

### 2.1 Backend（FastAPI + SQLAlchemy）

**只承载数据与基础能力。** 所有智能能力（解析 / 条款 / 元数据 / 规则 / LLM / 编排）都不在这里。

```
E:\python\contract_approval\
├─ backend\
│  ├─ pyproject.toml                 # 后端依赖
│  ├─ alembic.ini
│  ├─ migrations\versions\           # 【已实现】Alembic 迁移（baseline 已建 15 张表）
│  ├─ app\
│  │  ├─ main.py                     # 【已实现】FastAPI 实例、路由挂载、异常处理器、lifespan
│  │  ├─ core\
│  │  │  ├─ config.py                # 【已实现】pydantic-settings，读 .env
│  │  │  ├─ constants.py             # 【已实现】枚举：任务状态/风险等级/条款类型...
│  │  │  ├─ logging.py               # 【已实现】结构化 JSON 日志 + request_id 上下文
│  │  │  ├─ errors.py                # 【已实现】业务异常体系 ErrorCode + 错误分类
│  │  │  └─ executors.py             # 【已实现】线程池 / OCR 进程池（§1.3）
│  │  ├─ db\
│  │  │  ├─ base.py  session.py      # 【已实现】DeclarativeBase + async engine + session_scope
│  │  │  ├─ models\                  # 【已实现】15 张表，一张表一个文件（§7）
│  │  │  └─ repositories\            # 【待建】复杂查询封装
│  │  ├─ schemas\
│  │  │  ├─ contract.py              # 【已实现】ContractIngestResponse
│  │  │  └─ (task / document / risk / rule ...)   # 【待建】
│  │  ├─ api\
│  │  │  ├─ health.py                # 【已实现】GET /health（含 DB 连通性探测）
│  │  │  └─ v1\
│  │  │     ├─ router.py             # 【已实现】汇总挂载 prefix=/api/v1
│  │  │     └─ endpoints\contracts.py# 【已实现】POST /contracts（上传接入）
│  │  ├─ services\
│  │  │  ├─ contract_ingest.py       # 【已实现】接入编排 + 两层幂等 + 补偿清理
│  │  │  └─ (review_task / human_review ...)      # 【待建】
│  │  ├─ storage\                    # 【已实现】StorageBackend + LocalStorageBackend
│  │  ├─ utils\                      # 【已实现】file_utils / hash_utils / datetime_utils
│  │  ├─ integrations\               # 【待建】ApprovalClient（Mock/Real 同接口）
│  │  ├─ parsers\                    # 【空目录】⚠️ 归属已变更到 Agent，**不要在这里实现解析器**
│  │  └─ llm\  rules\  reports\  workers\          # 【空目录/待建】同理，见下方归属对照
│  ├─ scripts\seed_rules.py          # 【已实现】灌入预置规则集
│  ├─ tests\unit\  tests\integration\# 【已实现】342 passed
│  └─ storage\                       # 运行时文件（gitignore）
│     ├─ uploads\{contract_id}\      # 原始文件
│     ├─ renders\{file_id}\          # OCR 页面图（PDF/扫描件）
│     └─ reports\                    # 【P12 定案：不落盘】报告实时生成、直接下发，无产物文件
├─ ai-agent\                         # 见 2.2
├─ frontend\                         # 见 2.3
├─ samples\                          # 示例合同
├─ docs\                             # 本设计文档与后续阶段文档
├─ pyproject.toml                    # uv workspace 根（members = backend, ai-agent）
├─ .env.example
└─ README.md
```

**能力归属对照表（旧目录 → 新归属）**

| 旧位置 | 现在归谁 | 说明 |
|---|---|---|
| `backend/app/parsers/` | **AI Agent** `app/parsers/` | 文档解析是 Agent 的职责。Backend 侧保持空目录 |
| `backend/app/llm/` | **AI Agent** `app/llm/` | Prompt / Provider / 结构化输出 |
| `backend/app/rules/` | **AI Agent** `app/rules/` | 规则求值属于审查流程；**规则数据的 CRUD 仍在 Backend**（数据 vs 引擎，见下） |
| `backend/app/reports/` | ~~**AI Agent** `app/reports/`~~ → **Backend** | ⚠️ **P12 修正**：报告**不是**审查流程的产物，而是对**已持久化审查结果**的只读投影。实现落在 `backend/app/services/report_query.py`（查询）+ `report_render.py`（纯函数渲染）+ `api/v1/endpoints/reports.py`（下载）。Agent 侧**没有** `app/reports/`、没有 `generate_report` 节点 |
| `backend/app/workers/` | **暂缓**（见 §17） | 当前 Agent 在 HTTP 请求内同步跑完 Graph，尚不需要 DB 队列 |

> ⚠️ **规则这一项最容易搞混**：`review_rule` / `review_rule_set` 的**数据与 CRUD API 属于 Backend**（它们是业务数据），
> 而"拿规则去逐条评估合同文本"的**求值引擎属于 Agent**（它是智能流程的一部分）。
> 判断标准仍是 §1.1 的那一句：**数据归 Backend，智能归 Agent**。

### 2.2 AI Agent（FastAPI + LangGraph）

**智能审查流程的唯一归属地。**

```
ai-agent\
├─ pyproject.toml                    # ⚠️ 刻意不含 SQLAlchemy / aiomysql
└─ app\
   ├─ main.py                        # 【已实现】FastAPI 实例 + lifespan（BackendClient / 编译好的 Graph）
   ├─ core\
   │  ├─ config.py                   # 【已实现】AgentSettings（无任何数据库配置）
   │  └─ errors.py                   # 【已实现】AgentErrorCode
   ├─ api\
   │  └─ review.py                   # 【已实现】POST /api/agent/review（编排入口）
   ├─ graph\                         # ★ LangGraph 编排层
   │  ├─ state.py                    # 【已实现】ContractReviewState
   │  ├─ context.py                  # 【已实现】ReviewContext（Runtime Context 依赖注入）
   │  ├─ builder.py                  # 【已实现】StateGraph 装配与编译
   │  ├─ nodes\
   │  │  ├─ upload_file.py           # 【已实现】调 Backend 接入接口
   │  │  ├─ validate_file.py         # 【已实现】Workflow Gate
   │  │  ├─ parse_document.py        # 【已实现】StubParser 桩实现
   │  │  └─ (identify_clauses / extract_metadata / extract_keywords /
   │  │      rule_review / llm_review / merge_risks /
   │  │      generate_suggestions / persist_result)   # 【待建】
   │  └─ edges\routing.py            # 【已实现】Conditional Edge 判断函数
   ├─ tools\
   │  └─ backend_client.py           # 【已实现】Backend 领域 API 客户端（httpx）
   ├─ schemas\
   │  ├─ document.py                 # 【已实现】ParseResult
   │  └─ review.py                   # 【已实现】ReviewRunResponse
   ├─ parsers\                       # 【待建】DocumentParser 抽象 + DocxParser ...
   ├─ understanding\                 # 【待建】条款识别 / 元数据 / 关键词
   ├─ rules\                         # 【待建】规则求值引擎
   ├─ llm\                           # 【待建】LLMProvider + Prompt 模板
   ├─ reports\                       # 【P12 定案：不建】报告不由 Agent 渲染，改由 Backend 实时生成
   └─ tests\unit\  tests\integration\# 【已实现】16 passed
```

### 2.3 Frontend（Vue3 + Vite）

```
frontend\
├─ package.json  vite.config.ts  tsconfig.json  index.html
├─ .env.development                 # VITE_API_BASE_URL=/api/v1
└─ src\
   ├─ main.ts  App.vue
   ├─ api\                          # 与后端 endpoint 一一对应的请求函数
   │  ├─ request.ts                 # Axios 实例：token 注入、错误码→提示、超时
   │  ├─ contract.ts  task.ts  risk.ts  rule.ts  approval.ts  report.ts
   │  └─ types\                     # 与后端 Pydantic schema 对齐的 TS 类型（唯一真源对照表）
   ├─ router\index.ts               # 路由 + 角色守卫
   ├─ stores\                       # Pinia：user / task / workspace
   ├─ composables\
   │  ├─ useHighlight.ts            # ★ 风险↔原文双向高亮联动核心
   │  ├─ useTaskPolling.ts          # 任务状态轮询（带退避）
   │  └─ useFileUpload.ts
   ├─ components\
   │  ├─ document\
   │  │  ├─ DocumentViewer.vue      # 容器：按文件类型切换渲染器
   │  │  ├─ PdfTextViewer.vue       # pdfjs-dist 文本层
   │  │  ├─ DocxPreviewer.vue       # docx-preview
   │  │  └─ HighlightLayer.vue      # 高亮覆盖层（风险/元数据/批注 三类）
   │  ├─ risk\
   │  │  ├─ RiskCard.vue  RiskLevelTag.vue  SuggestionPanel.vue
   │  │  └─ RiskFilterBar.vue
   │  ├─ workspace\
   │  │  ├─ MetadataPanel.vue  ClauseOutline.vue
   │  │  ├─ AnnotationEditor.vue  WritebackPanel.vue
   │  │  └─ SummaryCard.vue
   │  └─ common\ (StatusTag / EmptyState / PageHeader / UploadButton)
   ├─ views\
   │  ├─ LoginView.vue
   │  ├─ DashboardView.vue          # 审查大盘
   │  ├─ ContractListView.vue       # 合同列表
   │  ├─ ReviewWorkspaceView.vue    # ★ 智能审查工作台（双栏）
   │  ├─ RuleConfigView.vue         # 合规规则与模板
   │  ├─ StandardClauseView.vue     # 标准条款库
   │  ├─ MockApprovalView.vue       # Mock 审批系统
   │  └─ TaskMonitorView.vue        # 任务监控/阻塞处理（管理员）
   ├─ styles\  utils\  directives\
```

---

## 3. 模块之间的依赖关系

**跨服务依赖方向严格单向：Frontend → Agent → Backend → MySQL，禁止反向调用。**

```
┌─ Frontend ──────────────────────────────────────────────────────────┐
│  views ──► composables ──► api/(request.ts + 各资源函数) ──► HTTP     │
└──────────────────────┬──────────────────────────────────────────────┘
                       │ ① 发起审查 / 上传          ② 查询与人工审核
                       ▼                                    │
┌─ AI Agent ──────────┴────────────────────────────────────┼─────────┐
│  api/review.py ──► graph/builder (LangGraph)             │         │
│                        │                                 │         │
│        ┌───────────────┼───────────────┐                 │         │
│        ▼               ▼               ▼                 │         │
│   graph/nodes ──► graph/edges      graph/state           │         │
│        │                                                 │         │
│        ├──► parsers / understanding / rules / llm / reports  【待建】│
│        │                                                 │         │
│        └──► tools/backend_client ──── httpx ─────────────┼─────────┤
└──────────────────────────────────────────────────────────┼─────────┘
                                                           ▼
┌─ Backend ───────────────────────────────────────────────────────────┐
│  api/v1/endpoints ──► services ──► {storage, integrations, reports}  │
│                          │                                          │
│                          └──► db/repositories ──► db/models ──► MySQL│
└─────────────────────────────────────────────────────────────────────┘
```

**Backend 内部规则**

| 规则 | 说明 |
|---|---|
| endpoints **只**做参数校验 + 调 service + 组装响应 | 禁止在 endpoint 里写业务分支、禁止直接查库 |
| services 之间可以互相调用，但**任务状态变更只能走 `ReviewTaskService`** | 防止状态机被绕过 |
| `storage/` 不依赖 services | 保证存储后端可替换、可单测 |
| `integrations/` 只暴露 `ApprovalClient` 接口 | Mock 与真实实现同签名，切换只改配置 |
| 幂等是**领域规则**，写在 `services/contract_ingest.py`，不散落到 endpoint | 一处定义、一处修改 |

**Agent 内部规则**

| 规则 | 说明 |
|---|---|
| `graph/nodes/*` 只负责"读 State → 做事 → 写 State" | 编排逻辑集中在 `builder.py`，节点不互相调用 |
| 节点**不直接抛业务异常**，失败写进 State 交给 Conditional Edge | 抛异常会中断整张图，条件边就失去意义 |
| `Runtime Context` 承载依赖（HTTP 客户端等），**不进 State** | State 只放业务数据；依赖在 `ainvoke(context=...)` 时注入 |
| `parsers` / `rules` / `llm` / `reports` **不依赖 graph** | 保证能力域可单测、可替换 |
| `llm/` 不感知数据库，输入输出都是纯 Pydantic 对象 | 便于 Mock 测试、便于换模型 |
| 所有数据读写**只能**经 `tools/backend_client.py` | Agent 侧唯一的数据出入口，禁止绕过 |

**关键交叉依赖**

- `graph/nodes/upload_file.py` → `tools/backend_client.upload_contract()` → `POST /api/v1/contracts`
- `graph/nodes/parse_document.py` → `parsers/*` → State 里的 `parse_result`（**不落库**，落库由 `persist_result` 经 Backend API 完成）
- `graph/nodes/rule_review.py` → `rules/engine` + 规则数据（经 Backend API 取）
- `graph/nodes/llm_review.py` → `llm/provider` + `llm/prompts/*`
- `graph/nodes/persist_result.py` → `tools/backend_client` → Backend 的持久化 API
- `frontend/api/types` ↔ `backend/schemas`：**字段名一一对应，是一份契约的两侧**
- `frontend/api/*` ↔ `ai-agent/app/schemas/review.py`：**Agent 也有自己的对外契约**，前端调 Agent 的那部分以它为准

**⚠️ 反面例子：什么叫"两套实现"**

```
❌ 错误：Agent 的 upload_file 节点自己实现扩展名/MIME/魔数校验 + SHA256 + 幂等
        → 与 Backend 的 contract_ingest 形成两套会各自漂移的规则

✅ 正确：Agent 的 upload_file 节点调 POST /api/v1/contracts，把 Backend 的返回放进 State
        → 校验/哈希/幂等只有一份实现，Agent 侧只有"转发 + 翻译"
```

---

## 4. 数据流

### 4.1 主数据流（端到端）

**自动审查段（Agent 主导）** —— 当前已实现的部分用 ✅ 标注：

```
[业务经办人] 上传文件
        │
        │  POST /api/agent/review (multipart)      ← Frontend 调的是 Agent，不是 Backend
        ▼
┌─ AI Agent Service ───────────────────────────────────────────────────┐
│                                                                      │
│  ① upload_file ✅                                                     │
│     经 httpx → POST /api/v1/contracts                                 │
│     Backend 负责：校验类型/大小/魔数 → sha256 → 两层幂等 →            │
│                   落 contract + contract_file + review_task           │
│     Agent 负责：把返回的三个 ID 与 sha256 放进 State                   │
│                          │                                           │
│  ② validate_file ✅   Workflow Gate：产出完整到能继续吗？              │
│                          │                                           │
│              ┌───────────┴───────────┐                               │
│           invalid                    valid                           │
│              │                        │                              │
│              ▼                        ▼                              │
│             END              ③ parse_document ✅（StubParser 桩）     │
│                                     → 产出 ParseResult                │
│                          │                                           │
│  ④ identify_clauses      【待建】条款识别 → clause[]                   │
│  ⑤ extract_metadata      【待建】元数据提取 → metadata{}               │
│  ⑥ extract_keywords      【待建】关键词提取 → keywords[]               │
│                          │                                           │
│  ⑦ rule_review           【待建】规则引擎逐条评估（确定性、快）         │
│  ⑧ llm_review            【待建】LLM 语义研判（结构化 JSON）            │
│  ⑨ merge_risks           【待建】合并去重 → 风险评分 → 综合等级         │
│ ⑩ generate_suggestions   【待建】修改建议（LLM + 标准条款库匹配）       │
│ ⑪ （报告导出）           【P12 定案】由 Backend 实时生成             │
│                          │                                           │
│ ⑫ persist_result         【待建】经 Backend API 落库：                 │
│        document_block / contract_metadata / clause /                  │
│        risk_item / risk_suggestion                                   │
│                          │                                           │
│                         END                                          │
└──────────────────────────┬───────────────────────────────────────────┘
                           │ 返回最终 State 的投影（ReviewRunResponse）
                           ▼
[前端] 工作台：GET /tasks/{id}/document + /risks + /metadata
       原文高亮锚定（char_start_global / char_end_global）
```

**人工复核段（普通 Backend 业务，与 LangGraph 无关）**：

```
[法务审查人] 核对风险 → 确认 / 驳回 / 修改等级 → 采纳或编辑建议
        │
        │  PATCH /risks/{rid}  →  risk_item.review_status
        ▼
[可选] POST /tasks/{id}/writeback
        WritebackService：生成 Markdown 评论 → ApprovalClient.post_comment(幂等键)
        状态：not_written → writing → success / failed
        │
        ▼
[业务经办人] GET /api/v1/review-tasks/{id}/report/export
        ReportService：**Backend 基于已持久化的 ReviewTask 结果实时生成 Markdown**
        （只读投影；不重跑 LLM/规则/合并/解析，无 report 表、无"生成"步骤、无 report_id）
```

**两条边界要记住**

| 边界 | 位置 | 含义 |
|---|---|---|
| **同步/异步** | `POST /api/agent/review` 内部 | 当前版本 Agent 在**一次 HTTP 请求内**同步跑完整张图。当 LLM 审查耗时增长到 HTTP 不可接受时，再引入 DB 队列 + Worker（见 §17 扩展阶段） |
| **自动/人工** | `persist_result → END` 之后 | 人工审核**不在图里**，不使用 `interrupt()` / checkpoint。它只是 Backend 上的一次普通状态更新 |

### 4.2 数据流中的三个关键「转换点」

1. **文件 → 归一化文本流**：产生 `document_block` 序列，每个 block 同时持有 `page_number`、`paragraph_index`、block 内偏移、**全局字符偏移**。
2. **条款 → 风险**：LLM 输出只带 `quote`，**Agent** 在原文中反查回填全局偏移，保证「风险卡片 ↔ 原文」永远能对上。
3. **审查结论 → 评论 Markdown**：写回内容是可追溯的纯文本快照（含风险清单 + 法务批注），落 `writeback_record.content_md` 存档，便于审计与重放。

---

## 5. 核心业务流程

### 5.1 审批触发与自动审查（流程 A）

1. 触发源二选一：① 用户在系统内上传；② 定时/手动拉取 Mock（未来真实）审批系统待办。
2. 拉取时按 `sync_cursor` 增量拉取，**先查 sha256 去重**，已存在的附件不重复下载。
3. 建任务 → 返回 `task_id`，前端拿到后进入工作台轮询。
4. Worker 执行 PARSE → REVIEW 两阶段。
5. 完成后任务 `completed`，前端轮询到终态后自动刷新风险列表。

### 5.2 法务人工复核与协同改写（流程 B）

1. 法务在工作台打开合同，左侧原文 + 右侧风险卡片。
2. 点击风险卡片 → 原文自动滚动到对应位置并高亮（`useHighlight`）。
3. 法务对每个风险点可执行：**确认** / **驳回（误报）** / **调整风险等级** / **编辑建议文本** / **重新生成建议**。
4. 法务写人工批注（可锚定到某个 block 或某个风险点）。
5. 点击「写回审批意见」→ 系统生成摘要 Markdown（含风险总评 + 重点关注 + 法务批注）→ 调用审批评论接口。
6. 回写结果轮询展示；成功后业务经办人可下载整改对比报告。

### 5.3 异常阻塞与重试（流程 C）

1. PARSE 阶段不可恢复错误（加密、空正文、扫描严重模糊、格式不支持）→ `blocked`，记录 `block_reason_code` + 人话原因。
2. REVIEW 阶段系统级错误（LLM 连续超时/5xx 超阈值）→ 自动重试耗尽后 `blocked`。
3. 管理员在任务监控页看到阻塞列表 → 查看原因 → 可「修正文件/调整参数」→ 点击「重新审查」→ `blocked → pending`（`retry_count+1`）。
4. 重试时**断点续跑**：PARSE 阶段产物若已完整落库则跳过解析，直接进 REVIEW。

---

## 6. 状态机设计

### 6.1 任务状态机（review_task.status）

```
                    ┌──────────────────────────────────────┐
                    │                                      │
                    ▼                                      │
   [创建] ──►  pending ──► parsing ──► reviewing ──► completed(终态)
                 ▲            │            │
                 │            │            │
                 │            ▼            ▼
                 │         blocked ◄───────┘
                 │            │
                 └────────────┘  (管理员「重新审查」)
```

**合法迁移表（唯一权威，代码里用常量矩阵实现）**

| from \ to | pending | parsing | reviewing | blocked | completed |
|---|---|---|---|---|---|
| pending | – | ✅ worker 抢占 | ❌ | ✅ 前置校验失败 | ❌ |
| parsing | ✅ 重试 | ✅ 心跳续期 | ✅ 解析成功 | ✅ 解析失败/超时 | ❌ |
| reviewing | ✅ 重试 | ❌ | ✅ 心跳/分批评审 | ✅ 重试耗尽 | ✅ 审查完成 |
| blocked | ✅ 人工重试 | ❌ | ❌ | – | ❌ |
| completed | ❌（重新审查=新建 task） | ❌ | ❌ | ❌ | – |

**为什么 completed 不可回退**：审查结论是审计证据，必须不可变。需要重新审查时**新建一个 task**，`contract.current_task_id` 指向最新，历史任务与历史结论全部留存 —— 这是面试可讲的「审计友好」设计。

**阶段级断点字段**：`current_stage` ∈ {UPLOADED, PARSED, CLAUSED, REVIEWED}，用于断点续跑。

**状态变更的执行方式**（与 §1.4 对应）：
- `pending → parsing` 在**抢占事务 T1 内**完成（一条 UPDATE + 一条 task_event，随即 commit）。
- 其余迁移（`parsing→reviewing`、`→completed`、`→blocked`、`blocked→pending`）都是**独立短事务**，每条都是一个 `transition()` 调用 + 一条 task_event。
- 所有 UPDATE 都带 `WHERE id=:id AND status=:expected_from`（乐观校验）并 `version=version+1`，防止并发下状态被覆盖。

**block_reason_code 枚举**（枚举化才能做统计与自愈）：
`FILE_ENCRYPTED` / `FILE_CORRUPTED` / `UNSUPPORTED_FORMAT` / `EMPTY_TEXT` / `OCR_FAILED` / `OCR_LOW_QUALITY` / `LLM_UNAVAILABLE` / `LLM_SCHEMA_INVALID` / `TIMEOUT` / `INTERNAL_ERROR`

### 6.2 回写状态机（writeback_record.status）

```
not_written ──► writing ──► success (终态)
                   │
                   └──► failed ──► writing (重试, attempt+1)
```

| from \ to | writing | success | failed |
|---|---|---|---|
| not_written | ✅ 用户点击写回 | ❌ | ❌ |
| writing | – | ✅ 接口 2xx | ✅ 超时/4xx/5xx |
| failed | ✅ 重试 | ❌ | – |
| success | ❌ 幂等锁定 | – | – |

**幂等键**：`idempotency_key = sha256(task_id + content_md_hash)`。同内容重复提交，审批系统侧返回既有评论（Mock 会模拟这一行为），本地直接返回上次结果，不产生第二条评论。

### 6.3 风险复核状态（risk_item.review_status）

```
PENDING（AI 产出，未复核）
  ├─► CONFIRMED（法务确认）
  ├─► REJECTED （法务判定误报）
  └─► MODIFIED （法务调整了等级）
```

人工复核**只改这一条风险上的复核列**（`review_status` / `reviewer_id` /
`review_comment` / `reviewed_at`）。它不是流程的一环，也不产生综合结论：

| 人工复核**不是** | 说明 |
|---|---|
| **不是任务状态推进** | `review_task` 的 `current_stage` / `status` / `finished_at` / `risk_level_final` / `conclusion` / `summary` **都不因复核而变**。`REVIEWED` 的含义始终是"AI 风险的持久化已完成"，**不会**变成"人工复核完成" |
| **不是重算综合等级** | §11.2 的评分归 **Agent**，Backend 当前不计算它，也**不因为 `REJECTED` 而重算**任何综合等级 |
| **不扣除报告概览** | `REJECTED` 的风险**仍然计入**报告的风险概览 —— 概览统计的是 **AI 风险发现结果**（按当前 `risk_item.risk_level`），不是"人工裁决后剩下什么" |
| **不保存历史** | 采用 Scheme A 就地改写：`MODIFIED` 会**覆盖** AI 原始 `risk_level` 且**不可恢复**。没有 `original_risk_level`，也没有复核历史表 |

`reviewer_id` 当前**恒为 `NULL`** —— 系统尚未引入用户身份体系，也**不伪造**用户。

> 人工复核的结论只出现在**风险详情**（工作台卡片、报告的风险明细），
> 不参与任何综合裁决。

---

## 7. 数据库实体关系设计

### 7.1 ER 总览

```
sys_user ──1:N── contract(applicant)
contract ──1:N── contract_file          (sha256 唯一索引，用于附件去重)
contract ──1:N── review_task            (历史任务全留存；contract.current_task_id → 最新)
review_task ──1:N── task_event          (状态机审计时间线)
review_task ──1:N── risk_item
review_task ──1:1── writeback_record (最近一次；历史在 writeback_log)
review_task ──1:N── ai_call_log
contract ──1:N── document_block         (★ 原文坐标系)
contract ──1:N── clause                 (条款，由连续 block 组成)
contract ──1:N── contract_metadata      (★ 元数据项，带定位)
clause   ──1:N── risk_item              (风险挂在条款上)
risk_item ──1:N── risk_suggestion
risk_item ──0:N── annotation
contract ──1:N── annotation
rule_set ──1:N── rule
standard_clause ──0:1── risk_suggestion (建议引用的标准条款)
contract ──1:N── report

[Mock 域] approval_instance ──1:N── approval_comment
          approval_instance ──0:1── contract
```

### 7.2 核心表字段设计

> 约定：所有表含 `id BIGINT PK AUTO_INCREMENT`、`created_at`、`updated_at`（`DATETIME(3)`，UTC 存储）；所有文本列 `utf8mb4`；金额一律 `DECIMAL(18,2)`，**禁止 float**。

**contract 合同**
| 字段 | 类型 | 说明 |
|---|---|---|
| contract_no | VARCHAR(64) UNIQUE | 合同编号 |
| title | VARCHAR(255) | 合同名称 |
| contract_type | VARCHAR(32) | PURCHASE/SALES/SERVICE/LABOR/OTHER（决定用哪套规则集） |
| our_party / counterparty | VARCHAR(255) | 我方主体 / 相对方 |
| amount / currency | DECIMAL(18,2) / CHAR(3) | 金额与币种 |
| sign_date / effective_date / expire_date | DATE NULL | 日期三件套 |
| dept | VARCHAR(64) | 送审部门 |
| applicant_id | BIGINT FK→sys_user | 申请人 |
| source | VARCHAR(16) | UPLOAD / APPROVAL_SYSTEM |
| approval_instance_id | BIGINT NULL | 关联审批单 |
| current_task_id | BIGINT NULL | 最近一次审查任务（冗余，避免列表页 join） |
| status | VARCHAR(16) | 派生字段：跟随 current_task 状态，便于列表筛选 |

**review_task 审查任务**
| 字段 | 类型 | 说明 |
|---|---|---|
| contract_id / file_id | BIGINT | 一个合同可多次审查，历史任务全留存 |
| status | VARCHAR(16) | pending / parsing / reviewing / blocked / completed（§6.1） |
| current_stage | VARCHAR(16) | UPLOADED / PARSED / CLAUSED / REVIEWED，断点续跑依据 |
| progress | TINYINT | 0~100，前端进度条 |
| priority | INT DEFAULT 0 | 抢占排序用 |
| **worker_id** | VARCHAR(64) NULL | 抢占它的 worker 标识（§1.4） |
| **locked_at** | DATETIME(3) NULL | 抢占时刻 |
| **heartbeat_at** | DATETIME(3) NULL | 心跳时间，自愈扫描依据（§13.5） |
| **next_retry_at** | DATETIME(3) NULL | 退避重试的可见时间；NULL = 立即可抢 |
| **version** | INT DEFAULT 0 | 乐观锁版本号，每次状态变更 +1 |
| retry_count / max_retry | INT | 任务级重试计数（默认上限 3） |
| block_reason_code | VARCHAR(32) NULL | 阻塞原因枚举（§6.1），用于统计与自愈 |
| block_reason_msg | VARCHAR(512) NULL | 人话原因，前端直接展示 |
| risk_level_final / conclusion | VARCHAR(16) | 综合风险等级 / 结论 PASS\|RECTIFY\|REJECT |
| summary | TEXT NULL | 审查摘要（供报告与回写复用） |
| **idempotency_key** | VARCHAR(64) UNIQUE | `sha256(contract_id+file_sha256+rule_set_version+prompt_version)` |
| error_msg / started_at / finished_at | | 排查与统计用 |

索引：`idx_claim(status, next_retry_at, priority, id)`（抢占查询专用）、`idx_heartbeat(status, heartbeat_at)`（自愈扫描专用）

**contract_file 合同附件**
| 字段 | 说明 |
|---|---|
| contract_id | FK |
| file_name / file_ext / file_size | 原始信息 |
| **sha256** | **UNIQUE，附件去重与幂等的基石** |
| storage_path | 相对 `storage/uploads/{contract_id}/` 的路径 |
| page_count / is_scanned | 页数；是否判定为扫描件 |
| parse_status | 单文件解析状态 |

**document_block 文档段落块（★ 坐标系核心）**
| 字段 | 类型 | 说明 |
|---|---|---|
| contract_id / file_id | BIGINT | |
| order_index | INT | 全文档线性顺序，前端渲染顺序 |
| block_type | VARCHAR(16) | TITLE / PARAGRAPH / TABLE_ROW / HEADER / FOOTER |
| page_number | INT NULL | **仅真实页码**：PDF / 扫描 PDF / 图片有值；**DOCX 恒为 NULL，禁止估算填充**（§10.4） |
| paragraph_index | INT | DOCX 为真实段号；PDF / 图片为逻辑段号（该页内第 N 个文本块） |
| **locator_type** | VARCHAR(16) | `PAGE`（PDF/图片）/ `PARAGRAPH`（DOCX），解析阶段按文件类型一次性写入，**前端据此选择定位文案，不做隐式推断** |
| text | TEXT | **归一化后**文本 |
| raw_text | TEXT | 原文（OCR 原始输出/未归一化），审计用 |
| char_start_in_block / char_end_in_block | INT | block 内偏移（用于归一化映射） |
| **char_start_global / char_end_global** | INT | **全文档全局偏移区间 [start, end)** —— 所有高亮/定位的唯一依据 |
| bbox_json | JSON NULL | OCR/PDF 的坐标，用于渲染层精确框选 |
| ocr_confidence | FLOAT NULL | OCR 置信度 |

索引：`(contract_id, order_index)`、`(contract_id, char_start_global)`

**contract_metadata 元数据提取项**
| 字段 | 说明 |
|---|---|
| contract_id / task_id | |
| field_key | 如 `counterparty_name` / `credit_code` / `contract_amount` / `currency` / `perform_period` / `effective_condition` |
| field_label / field_value / value_type | 展示名 / 值 / 类型(TEXT/AMOUNT/DATE/CODE) |
| confidence | 抽取置信度 |
| source_block_id / char_start_global / char_end_global / page_number | **定位信息，前端元数据面板点击可跳原文** |
| extract_method | REGEX / LLM / MANUAL |
| verified_by / verified_at | 人工校对留痕 |

**clause 条款**
| 字段 | 说明 |
|---|---|
| contract_id / task_id | |
| clause_no / clause_type / title | 条款号（如「第三条」）/ 类型枚举 / 标题 |
| start_block_id / end_block_id | 组成范围 |
| char_start_global / char_end_global | 定位 |
| page_start / page_end | 页码范围 |
| text | 条款全文 |
| extract_method / confidence | RULE / LLM |

clause_type 枚举：`SUBJECT`(主体) `AMOUNT_PAYMENT`(金额支付) `ACCEPTANCE`(验收) `LIABILITY`(违约责任) `CONFIDENTIAL`(保密) `IP`(知识产权) `DISPUTE`(争议管辖) `FORCE_MAJEURE`(不可抗力) `DATA_SECURITY`(数据安全) `DELIVERY`(交付) `OTHER`

**review_rule_set / review_rule 规则**
| rule 字段 | 说明 |
|---|---|
| rule_code UNIQUE within set | 如 `LIAB_UNEQUAL_001` |
| rule_name / dimension / description | 展示用 |
| rule_type | KEYWORD / REGEX / EXISTS（该条款必须存在）/ MISSING（必备条款缺失）/ THRESHOLD（数值比较） |
| expression | **JSON**，如 `{"keywords":["无限责任","全部损失"],"logic":"ANY"}` 或 `{"field":"amount","op":"gt","value":1000000}` |
| target_clause_types | JSON 数组，限定作用范围 |
| severity | HIGH / MEDIUM / LOW |
| legal_basis | TEXT，法律依据（如《民法典》第 585 条违约金上限） |
| suggestion_template | 默认修改建议模板 |
| is_active / sort_order | |

**risk_item 风险项**
| 字段 | 说明 |
|---|---|
| task_id / contract_id / clause_id | |
| rule_id NULL | 来自规则时非空 |
| risk_code / risk_title / dimension | |
| risk_level | HIGH / MEDIUM / LOW |
| source | RULE / LLM / RULE+LLM |
| reason | 风险成因分析 |
| legal_basis | 法律合规依据 |
| original_text | 命中的原文片段（= quote 反查结果） |
| page_number / paragraph_index / **char_start_global / char_end_global** | **定位，前端高亮依据**（page_number 语义见 §10.4） |
| locator_type | PAGE / PARAGRAPH，与所属 contract_file 一致，前端定位文案依据 |
| **anchor_method** | `CLAUSE_SCOPED` / `BLOCK_SCOPED` / `DOC_GLOBAL` / `CLAUSE_FALLBACK`，取自 §10.5 四级作用域 |
| **anchor_score** | 定位置信度 0~1；< 0.8 时前端显示「⚠ 定位待核对」角标 |
| **occurrence_index** | 该 quote 在最终作用域内是第几次出现（消歧结果，1-based） |
| **quote_context** | JSON `{before, after}`，quote 紧邻上下文，用于消歧与人工复核 |
| confidence | LLM 对该风险判断本身的置信度（与 anchor_score 是两回事，不要混淆） |
| review_status | PENDING/CONFIRMED/REJECTED/MODIFIED |
| reviewer_id / review_comment / reviewed_at | 人工复核留痕 |

**risk_suggestion 修改建议**
| 字段 | 说明 |
|---|---|
| risk_item_id | |
| suggestion_type | REPLACE / ADD / DELETE |
| original_text | 原条款文本 |
| suggested_text | AI 生成的示范条款 |
| reason | 修改理由 |
| standard_clause_id NULL | 引用的标准条款库条目 |
| is_adopted / adopted_by / adopted_at / final_text | **法务编辑后的最终文本（回写报告用 final_text，不用 suggested_text）** |

**writeback_record 回写记录**
| 字段 | 说明 |
|---|---|
| task_id / contract_id / approval_instance_id | |
| content_md TEXT | 回写内容快照（审计证据） |
| content_hash | |
| status | not_written/writing/success/failed |
| attempt | 重试次数 |
| **idempotency_key** UNIQUE | 幂等 |
| external_comment_id | 审批系统返回的评论 ID |
| request_body / response_body / error_msg | 排查用（脱敏） |
| operator_id / started_at / finished_at | |

**ai_call_log 模型调用日志（★ 面试重点）**
`task_id, provider, model, scene(CLAUSE_REVIEW/METADATA/SUGGESTION/SUMMARY), prompt_version, prompt_tokens, completion_tokens, total_tokens, latency_ms, status, retry_index, http_status, error_code, error_msg, request_id, raw_response MEDIUMTEXT, created_at`

**approval_instance / approval_comment（Mock 域）**
`instance_no, title, applicant, dept, amount, status, contract_id` ／ `instance_id, author, content, comment_type(USER/SYSTEM_REVIEW), source(MANUAL/CONTRACT_REVIEW_SYSTEM), external_id UNIQUE, created_at`

**sync_cursor**：`source, cursor_value, last_sync_at` —— 增量拉取待办的游标。

---

## 8. 核心 API 分类

### 8.0 两套 API，分属两个服务

| 服务 | 前缀 | 面向 | 当前状态 |
|---|---|---|---|
| **AI Agent** | `/api/agent/**` | Frontend 发起审查 | `POST /api/agent/review` ✅ |
| **Backend** | `/api/v1/**` | Frontend 查询与人工审核；Agent 读写业务数据 | `GET /health`、`POST /contracts` ✅ |

**Agent API**

| 方法与路径 | 说明 | 状态 |
|---|---|---|
| `POST /api/agent/review` | 上传合同文件 + 业务元数据 → 跑完整张 LangGraph → 返回最终 State 投影。`200` = 走完全程；`422` = 被 Workflow Gate 拦下（原因见 `error_code`） | ✅ |
| `GET /health` | Agent 自身健康检查。**刻意不探测 Backend**，避免两个服务的探针互相耦合 | ✅ |

> ⚠️ Agent **不暴露**合同/任务/风险的查询接口 —— 那些数据的真源在 Backend，前端直接查 Backend。
> Agent 只提供"跑一次审查"这一个动作。这样两个服务的 API 面不会重叠。

### 8.1 Backend API

统一前缀 `/api/v1`，统一响应体 `{code, message, data, request_id}`，列表统一 `{items, total, page, page_size}`。

> **本轮明确不做认证**：JWT / RBAC / 登录接口**不在当前阶段范围内**（本项目为学习与面试项目，不引入当前不需要的复杂度）。下表保留该行仅作占位，实现时若确有必要再补。

| 分类 | 方法与路径 | 说明 |
|---|---|---|
| **认证** | ~~`POST /auth/login`、`GET /auth/me`~~ | **明确不做**（原 P4b，见 §17） |
| **合同** | `GET /contracts`（筛选：status/risk/type/keyword/日期）、`POST /contracts`（上传）、`GET /contracts/{id}`、`DELETE /contracts/{id}`、`GET /contracts/{id}/timeline` | 列表/详情/时间线 |
| **任务** | `POST /contracts/{id}/tasks`（发起或重新审查）、`GET /tasks`、`GET /tasks/{id}`、`POST /tasks/{id}/retry`、`POST /tasks/{id}/cancel` | |
| **原文** | `GET /tasks/{id}/document`（blocks 全量，按页分组）、`GET /tasks/{id}/document/pages/{page}` | 前端渲染 + 高亮依据 |
| **元数据** | `GET /tasks/{id}/metadata`、`PATCH /metadata/{mid}`（人工修正） | |
| **条款** | `GET /tasks/{id}/clauses` | 条款大纲/目录 |
| **风险** | `GET /tasks/{id}/risks`（按等级/维度/复核状态筛选）、`GET /risks/{rid}`、`PATCH /risks/{rid}`（确认/驳回/改等级） | |
| **建议** | `GET /risks/{rid}/suggestions`、`POST /risks/{rid}/suggestions`（生成/重新生成）、`PATCH /suggestions/{sid}`（编辑/采纳） | |
| **批注** | `GET /tasks/{id}/annotations`、`POST /tasks/{id}/annotations`、`DELETE /annotations/{aid}` | |
| **综合结论** | `GET /tasks/{id}/summary` | 总风险等级 + 结论(PASS/RECTIFY/REJECT) + 摘要 |
| **回写** | `POST /tasks/{id}/writeback`（Body 可 dry_run 预览）、`GET /tasks/{id}/writeback`、`POST /tasks/{id}/writeback/retry` | 幂等键走 Header `Idempotency-Key` |
| **报告** | `GET /api/v1/review-tasks/{task_id}/report/export` | **Backend 基于已持久化结果实时生成 Markdown** 并下载。无 `POST`（没有"生成"这一步）、无 `report_id`、无预览接口。`current_stage != REVIEWED` → 409 `REPORT_NOT_READY`；重复下载无副作用 |
| **规则** | `GET/POST /rule-sets`、`GET/PUT/DELETE /rule-sets/{id}`、`GET/POST /rule-sets/{id}/rules`、`PUT/DELETE /rules/{id}`、`POST /rules/{id}/test`（对样例文本试跑） | `test` 接口是亮点：规则改动即时验证 |
| **标准条款** | `GET/POST /standard-clauses`、`PUT/DELETE /standard-clauses/{id}` | |
| **大盘** | `GET /dashboard/stats`（各状态计数/各风险等级计数/平均审查时长）、`GET /dashboard/trend` | |
| **Mock 审批** | `GET /mock-approval/instances`、`POST /mock-approval/instances`、`GET /mock-approval/instances/{id}/comments`、`POST /mock-approval/instances/{id}/comments`、`POST /contracts/pull-from-approval`、`POST /mock-approval/fault-config`（故障注入开关） | 最后一个是演示阻塞重试的关键 |
| **管理** | `GET /admin/tasks/blocked`、`GET /admin/llm-logs`、`GET /admin/health` | |

**接口约定（第二阶段会固化成文档）**：
- 长耗时操作一律 **202 Accepted + task_id**，前端轮询 `GET /tasks/{id}`；轮询用退避（1s→2s→3s→5s 封顶）。
- 写操作支持 `Idempotency-Key` Header。
- 所有错误响应带业务 `code`（如 `TASK_BLOCKED`、`WRITEBACK_FAILED`），前端据此映射提示，不靠 message 字符串匹配。

---

## 9. AI 审查流程

> ### ⚠️ 归属说明（本节设计仍然有效，执行者已变更）
>
> **LLM 调用与 Prompt 属于 AI Agent，不属于 Backend。**
> 本节的 Provider 抽象、结构化输出防线、Prompt 版本管理**一条都没有作废**，
> 只是执行位置从 `backend/app/llm/` 移到了 **`ai-agent/app/llm/`**。
>
> * Backend 侧的 `app/llm/` **保持空目录**，不要在那里实现 Provider
> * Agent **不读数据库**取规则/条款：这些数据经 `tools/backend_client.py` 调 Backend API 拿到
> * LLM 的每次调用留痕（`ai_call_log`）由 Agent 经 Backend API 写入 —— 表在 Backend，写入走 API

### 9.1 LLM Provider 抽象设计（你特别要求的）

```
                    ┌──────────────────────────────┐
                    │      LLMProvider (ABC)       │
                    │──────────────────────────────│
                    │ chat(req: LLMRequest)        │
                    │   -> LLMResult               │
                    │ health_check() -> bool       │
                    │ supports_structured_output() │
                    └───────┬──────────────┬───────┘
                            │              │
              ┌─────────────▼───┐   ┌──────▼─────────────┐
              │ DeepSeekProvider│   │ AnthropicProvider  │
              │ (OpenAI 兼容协议)│   │ (官方 anthropic SDK)│
              │ 默认结构化输出:   │   │ 结构化输出:          │
              │ json_object 模式 │   │ output_config.format│
              └─────────────────┘   └────────────────────┘
                            │
                    ┌───────▼──────────────────────┐
                    │ LLMFactory.build(settings)   │  ← 按配置切换，业务代码零改动
                    └──────────────────────────────┘
```

**统一请求/响应契约**

```python
class LLMRequest(BaseModel):
    scene: str                  # CLAUSE_REVIEW / METADATA_EXTRACT / SUGGESTION / SUMMARY
    system_prompt: str
    user_prompt: str
    output_schema: type[BaseModel] | None   # 非空 = 要求结构化输出
    temperature: float = 0.2
    max_tokens: int = 4096
    timeout: float = 120.0
    prompt_version: str          # 写入 ai_call_log，便于效果回归对比

class LLMResult(BaseModel):
    parsed: BaseModel | None     # ✅ 已通过 Pydantic 校验的结构化对象
    raw_text: str                # 原始文本，留痕
    model: str; provider: str
    prompt_tokens: int; completion_tokens: int; latency_ms: int
    request_id: str
    attempts: int                # 实际尝试次数（含 JSON 修复重试）
```

**统一结构化输出保障链（`json_guard.py`，四道防线）**

1. **协议层**：DeepSeek 用 `response_format={"type":"json_object"}`；Anthropic 用 `output_config={"format": {...}}`（结构化输出，比让模型"自己注意 JSON"可靠得多）。
2. **提示层**：prompt 内嵌 JSON Schema 与字段说明 + 3 条硬性规则（只输出 JSON、不得编造条款、quote 必须逐字复制原文）。
3. **解析层**：剥 ```json 围栏 → `json.loads` → 失败则用「错误信息 + 原始输出」回灌模型做一次修复重试（≤2 次）。
4. **校验层**：Pydantic 严格校验（字段缺失/枚举越界/类型错误一律抛 `LLM_SCHEMA_INVALID`），校验失败同样走修复重试；**仍失败则本批降级为「仅规则引擎结果」并在任务上标记 warning，绝不让整个任务失败**。

> **阶段归属（与 §17 对齐）**：本节全部内容归属 **AI Agent 的 `app/llm/`**（不是 Backend）。
> 第 **1/2/4** 道防线属于 **P9（LLM 审查），必须实现** —— 协议层约束 + 提示层 schema 说明 + Pydantic 校验，共同保证 LLM 输出是结构化 JSON 对象而非自然语言，这是前端可解析的前提。
> 第 **3** 道防线中的"JSON 修复重试"，以及失败重试、分批并发、Semaphore 限流**属于扩展阶段（黄金链路验收之后）**。
> P9 阶段 JSON 解析失败即视为该批次失败并如实记录，不做自动修复 —— 先跑通，再加固。

**模型配置（不写死在代码里）**

| Provider | 配置项 | 说明 |
|---|---|---|
| DeepSeek（主力） | `LLM_PROVIDER=deepseek`、`DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL` | 你环境里已配置的 V4.1 Flash 走这里；**模型名从环境变量读，代码里不硬编码**（模型换代时只改 `.env`） |
| Anthropic（备选/对照） | `LLM_PROVIDER=anthropic`、`ANTHROPIC_API_KEY`、`ANTHROPIC_MODEL=claude-opus-5` | 用**官方 `anthropic` SDK**，不要用 OpenAI 兼容 shim。适合做「同一批条款两个模型跑对照」的加分演示 |

> 为什么坚持抽象而不直接调一个 SDK：① 需求要求可替换；② 面试时能讲「模型可插拔、prompt 有版本号、调用有全量日志、失败有降级」这一整套工程化，而不是"我调了个 API"。

### 9.2 审查编排流程（两阶段混合）

**为什么规则 + LLM 混合**：规则引擎**确定性、可解释、零成本、可审计**，适合硬性红线（必备条款缺失、管辖地违规、违约金比例超限）；LLM 适合语义研判（权责是否对等、表述是否模糊、隐含风险）。只用 LLM 会漏掉硬规则且成本高；只用规则无法理解语义。**面试标准答案就是这句。**

```
条款集 (clauses)
   │
   ├─► 2a 规则引擎（并行，毫秒级）
   │     load rule_set(contract_type, is_active)
   │     for rule: evaluator.evaluate(rule, clause, metadata) → RuleHit | None
   │     产出：确定性风险（severity 已定）
   │
   ├─► 2b LLM 语义审查（分批并发，秒~分钟级）
   │     分批：按 clause_type 聚合，每批 ≤ 8 条款 且 ≤ 6000 字符
   │     上下文注入：合同类型 + 我方立场(甲方/乙方) + 该批条款文本(带 clause_id)
   │                    + 规则已命中的提示（让 LLM 不重复报，专注语义）
   │     并发：asyncio.Semaphore 限流（默认 4），单批失败不拖垮整任务
   │     产出：findings[]（含 level/reason/legal_basis/quote/context_before/
   │                       context_after/occurrence_hint/suggestion）
   │
   ├─► 2c 合并与去重
   │     key = (clause_id, dimension, 归一化 risk_title)
   │     同时命中 → source=RULE+LLM，level 取二者较高，reason 合并
   │     LLM 的 quote → 后端按 §10.5 四级作用域算法反查坐标
   │       ★ LLM 永不输出坐标；重复文本必须消歧；找不到时降级到条款级并标记待核对
   │       ★ 坐标反查先于落库，反查结果（anchor_method/anchor_score）一并写入 risk_item
   │
   ├─► 2d 风险评分（scorer.py，纯函数、可单测）  【待建 —— 见下方说明】
   │     score = Σ(HIGH:40, MEDIUM:15, LOW:5)，封顶 100
   │     综合等级 = max(所有风险等级)   ← 高风险一票定级，符合需求 §2.4.4
   │     结论：存在 HIGH → REJECT(建议拒绝)；仅 MEDIUM → RECTIFY(需整改)；
   │           仅 LOW/无 → PASS(可通过)
   │     ⚠️ 本步**尚未实现**：Backend 不代劳。人工复核（§6.3）也不触发它 ——
   │        REJECTED 不从任何统计中扣除，复核不写 review_task 的任何结论列
   │
   └─► 2e 建议生成
         优先取 LLM findings 内联的 suggestion
         高风险项额外匹配 standard_clause（按 clause_type + tags 相似度）作为示范条款
         无匹配则用 rule.suggestion_template
         最终写入 risk_suggestion（suggested_text + standard_clause_id）
```

### 9.3 Prompt 版本管理

`llm/prompts/clause_review.v1.md` 之类的文件，**文件名带版本号**，渲染前注入 schema。每次调用把 `prompt_version` 写进 `ai_call_log`。这样"改了 prompt 之后效果变好还是变差"有据可查，也便于把老版本任务重放对比。

---

## 10. 文档解析流程

> ### ⚠️ 归属说明（本节算法仍然有效，执行者已变更）
>
> **解析属于 AI Agent，不属于 Backend。** 本节描述的解析、归一化、坐标系、quote 反查算法
> **一条都没有作废**，只是执行位置从 `backend/app/parsers/` 移到了 **`ai-agent/app/parsers/`**。
>
> * Backend 侧的 `app/parsers/` **保持空目录**，不要在那里实现解析器
> * 第 0 步的"前置校验 + SHA256 + 附件去重"**已经由 Backend 在接入时完成了**（见 §4.1 ①），
>   Agent 的 `parse_document` 拿到的是已经落库、已经去重过的 `file_id`，**不重复做这一步**
> * 解析产物**不直接落库**：Agent 把它们放进 State，由末端 `persist_result` 节点经 Backend API 写入

### 10.1 解析流程图

```
file (docx / pdf / jpg / png)   ← 已由 Backend 接入并去重，Agent 拿到的是 file_id
  │
  ├─ 0 前置条件（**不由 Agent 做**）
  │    扩展名 + 魔数 + 大小上限(50MB) —— Backend 在 POST /contracts 时已完成
  │    sha256 去重 —— Backend 的 contract_file.sha256 UNIQUE 已完成
  │    Agent 只判断："我可以解析这个 file_type 吗？"
  │
  ├─ 1 分派
  │    DOCX ──► DocxParser
  │    PDF  ──► PdfParser（先试文本层）
  │    IMG  ──► OcrEngine 直通
  │
  ├─ 2 文本抽取
  │    DOCX: python-docx 遍历 body 元素（段落 + 表格，保持文档顺序）
  │          → block_type=PARAGRAPH/TABLE_ROW，paragraph_index 用真实段号
  │    PDF : PyMuPDF page.get_text("blocks") → 带 bbox
  │          判定扫描件：单页有效字符 < 50 且 (图像覆盖率 > 阈值) ⇒ 视为扫描页
  │          扫描页 → 该页渲染 300dpi PNG → OCR
  │          ⇒ 混合 PDF（部分文字版 + 部分扫描）按页分别处理，这点很多实现会漏
  │    OCR : RapidOCR 输出 [{box, text, score}]
  │          → 按 y 轴聚类成行、按行距聚类成段 → 生成 block
  │          → 记录 bbox + ocr_confidence
  │          → 全文平均置信度 < 0.6 ⇒ blocked(OCR_LOW_QUALITY)
  │
  ├─ 3 归一化（normalizer.py）★
  │    去页眉页脚（跨页重复出现的短行，出现 ≥3 页即判定）
  │    合并 OCR 断行（行尾无标点且下一行首非编号 ⇒ 拼接）
  │    全角/半角、中文空格、连字符统一
  │    保留 raw_text，并生成 normalized_index → raw_index 映射表
  │    ⚠️ 映射表必须在内存中随 block 一起产出：前端高亮用的是归一化坐标，
  │       若用户要"复制原文"，需要用映射表还原，否则复制的字会错位
  │
  ├─ 4 元数据提取（metadata_extractor.py）
  │    规则优先：正则/关键词 → 统一社会信用代码(18位)、金额(¥/元/万元 + 大写)、
  │              币种、日期(YYYY年M月D日 等 6 种格式)、履行期限、生效条件
  │    规则未覆盖字段 → 一次 LLM 调用（结构化输出）兜底，标注 extract_method=LLM
  │    每项都回填 source_block_id + char_start_global + char_end_global + page_number
  │
  ├─ 5 条款切分（clause_segmenter.py）
  │    规则优先：编号模式（第[一二三...]条 / ^\d+\.\d* / ^[一二三]、/ 第X章）
  │             命中 ≥ 3 条 ⇒ 视为结构化合同，走规则切分
  │    规则失败/命中过少 ⇒ LLM 兜底：
  │             把 block 序列（order_index + 前 40 字）给 LLM，要求返回
  │             [{clause_no, title, start_order_index, end_order_index, clause_type}]
  │             后端按 order_index 换算成 block 区间与全局字符区间
  │    兜底再失败 ⇒ 降级为「一段一 clause」，clause_type=OTHER，不阻塞任务
  │
  └─ 6 落库 & 状态推进
        document_block / contract_metadata / clause 批量写入
       current_stage=PARSED → task: parsing → reviewing
```

### 10.2 统一坐标系（全文最重要的设计）

```
document_block:
  order_index=12  page_number=3  paragraph_index=27
  text="第三条 违约责任：任何一方违约，应赔偿对方因此遭受的全部损失…"
  char_start_global=8421   char_end_global=8489

risk_item:
  char_start_global=8452   char_end_global=8470   ← "全部损失"
  page_number=3  paragraph_index=27
```

前端拿 `char_start_global/char_end_global` 做高亮，拿 `page_number` 跳页，拿 `paragraph_index` 在 DOCX DOM 中定位。**全系统只有这一套坐标**，风险/元数据/批注/建议全部复用，避免"四套坐标互相对不上"的经典翻车。

### 10.3 高亮为什么不会错位（面试必答）

1. 坐标系是 **Agent 解析时生成的、稳定的全局偏移**，不依赖前端渲染。
2. LLM 只允许输出 `quote` 原文片段，**不允许输出坐标**；后端用 `text.find(quote)` 反查，找不到就丢弃该条。
3. DOCX 前端用 `docx-preview` 渲染，其段落 DOM 顺序 = 解析时的 `order_index` 顺序，按 `paragraph_index` 索引即可定位（同一文档只解析一次，顺序确定性成立）。
4. PDF 前端用 `pdfjs-dist` 的文本层，`page_number` 定位到页，`char_start_global - 该页首字符全局偏移` 定位到页内字符区间。

### 10.4 DOCX 页码语义（MVP 明确定义）

**第一版绝不伪造 Word 原始页码。** 页码语义按文件类型严格区分：

| 文件类型 | `page_number` | 定位主键 | 前端展示 |
|---|---|---|---|
| PDF（文本层） | ✅ **真实页码**（PyMuPDF 页序，1-based） | `page_number` + 全局偏移 | 「第 N 页 / 共 M 页」，支持页码跳转 |
| PDF（扫描件，走 OCR） | ✅ **真实页码**（渲染时的页序） | 同上 | 同上 |
| 图片（jpg/png） | ✅ 恒为 1 | `paragraph_index` 为主 | 「单页图片」 |
| **DOCX** | ❌ **恒为 NULL** | **`order_index` + `paragraph_index`** | 「第 N 段」+ 自定义视图分页，**禁止出现任何"第 N 页"文案** |

**DOCX 的三条硬性约束：**

1. **后端**：`DocxParser` 一律写 `page_number = NULL`，**不得用任何估算值填充**。"每 40 段算一页"这类假分页会让页码与 Word 实际排版不符，法务按页码翻找不到内容 —— 这比没有页码更糟。
2. **前端**：`DocxPreviewer.vue` 用 `docx-preview` 连续流式渲染，**不显示页码**；顶部显示 `第 N 段 / 共 M 段`（由 `paragraph_index` 驱动）。若为阅读舒适需要分页视图，分页条必须显式标注 **「视图分页（非 Word 页码）」**，且分页位置只由前端视口宽度决定，与后端数据无关。
3. **定位文案按 `locator_type` 分支，不做隐式兜底**：
   - `locator_type = PAGE`：`第 3 页 · 第 27 段`
   - `locator_type = PARAGRAPH`：`第 27 段`（**不写页**）
   - 前端读后端返回的 `locator_type` 字段选择文案，**禁止**用 `page_number is null` 这种隐式判断（否则将来接入其他格式会立刻失效）。

`locator_type` 由 `contract_file.file_ext` 在解析阶段一次性写入（同一文件内恒定），并向下传递到 `risk_item`，前端无需自行推断。

**升级路径（第二版，明确未启用）**：若确需 Word 真实页码，`DocxParser.convert_to_pdf()` 通过 LibreOffice headless（`soffice --headless --convert-to pdf`）把 DOCX 转 PDF，用转换后 PDF 的页序回填 `page_number`。**注意这仍是"LibreOffice 排版下的页码"，与微软 Word 的排版可能有 ±1 页差异**，因此即便启用，前端也应标注为「参考页码」。第一版标记为增强项，不实现。

### 10.5 quote → 坐标反查算法（定位准确性核心）

**问题**：LLM 只输出 quote，坐标由 **Agent** 反查。但"甲方应于收到货物后 30 日内支付"这类句子在合同里可能重复出现多次，**简单地全文 `text.find()` 会高亮到错误位置**（例如高亮到附件或前文引用，而非真正的风险条款）。

**设计目标：定位必须在语义所属范围内确定；重复文本必须消歧；找不到时降级，绝不错误高亮。**

#### LLM 输出契约（findings 元素新增 3 个定位辅助字段）

```json
{
  "clause_id": 42,
  "risk_title": "付款条件缺失验收前置",
  "level": "HIGH",
  "reason": "...",
  "legal_basis": "...",
  "quote": "甲方应在收到货物后 30 日内支付全部货款",
  "context_before": "第四条 付款方式：",
  "context_after": "乙方应在发货前提供全额发票。",
  "occurrence_hint": 1,
  "suggestion": { "type": "REPLACE", "text": "...", "reason": "..." }
}
```

| 字段 | 必填 | 约束 |
|---|---|---|
| `quote` | ✅ | **必须逐字复制**所属 clause 的文本，禁止改写、禁止用省略号截断 |
| `context_before` / `context_after` | ✅ | quote 紧邻的前后文，各 ≤ 30 字，同样逐字复制 |
| `occurrence_hint` | ❌ | 模型认为这是第几次出现（1-based）。**仅作参考，不作唯一依据** |

#### 四级搜索作用域（由紧到松）

| 级别 | 搜索范围 | anchor_method | 采纳条件 |
|---|---|---|---|
| **L1** | 该 `clause_id` 的 `[char_start_global, char_end_global)` | `CLAUSE_SCOPED` | 作用域内**精确唯一命中**直接采纳；多次命中交 context 消歧 |
| **L2** | 该 clause 覆盖的全部 `document_block` 区间 | `BLOCK_SCOPED` | 同上（L1 未命中或消歧失败时进入） |
| **L3** | 全文（带边界分隔符，见下） | `DOC_GLOBAL` | **仅接受全文唯一命中**；多次命中时只有消歧结论明确才采纳 |
| **L4** | 定位失败 → 降级 | `CLAUSE_FALLBACK` | 条款级定位 + 「定位待核对」，**不做整条高亮** |

#### 裁决顺序（严格按此优先级，命中即返回）

```
① 精确唯一命中                 在 L1 内，其次 L2 内 → 采纳，anchor_score = 1.0
② context 消歧                 作用域内多次命中时启用打分（见下）
                                  分差达标 → 采纳
                                  分差不足 → 进入下一级作用域（L1 失败转 L2，L2 失败转 L3）
③ 全文唯一命中                 L3 内精确且唯一 → 采纳（anchor_method = DOC_GLOBAL）
④ fuzzy 模糊匹配（最后降级候选）
                                仅在 ①②③ 全部失败后启用；
                                相似度 ≥ 0.85 且唯一 → 采纳，anchor_score × 0.9
⑤ 全部失败 → CLAUSE_FALLBACK    条款级定位 + 「定位待核对」
```

**关键约束：fuzzy 永远不是主路径。** 它是精确匹配全部失败后的最后降级候选，且必须满足"唯一"才采纳；fuzzy 命中同样只降级、不猜测。

#### context 消歧打分（仅在第 ② 步使用）

```
同作用域内候选数 n：

n == 1  → 属于第 ① 步（精确唯一命中），直接采纳

n > 1   → 对每个候选 pi 计算上下文相似度：
            0.6 × context_before 在 pi 左侧 60 字窗口内的匹配长度比
          + 0.4 × context_after  在 pi 右侧 60 字窗口内的匹配长度比
          （匹配用最长公共前后缀，容忍归一化造成的个别字符差异）

          取最高分 max_score：
            max_score >= 0.8 且与第二名分差 >= 0.3  → 采纳，anchor_score = max_score
            否则                                    → 本作用域判定失败，进入下一级
```

> **第一原则：宁可降级，不可错标。** 错误高亮（把 A 条款的风险标到 B 条款上）对法务是灾难性的信任损失；而"定位到条款级 + 提示人工核对"是可接受且有礼貌的降级。

#### CLAUSE_FALLBACK 的展示语义（不做整条高亮）

定位失败时**不得**把整条 clause 渲染成风险高亮 —— 那等于向法务宣称"整条都有问题"，本身就是错误信息。正确做法：

- 定位降级到**条款级**：点击风险卡片跳转到该条款的**起始位置**；
- 卡片与高亮层标注 **「⚠ 定位待核对」**；
- 高亮层只用**弱化样式**（左侧边距标记 / 虚线边框）圈出条款范围，**不使用任何风险等级色**，与真正的风险高亮在视觉上明确区分；
- `anchor_score = 0.3`、`anchor_method = CLAUSE_FALLBACK`，计入管理端统计。

#### quote 找不到的全部情形与处理

| 情形 | 判定 | 处理 | 日志级别/码 |
|---|---|---|---|
| quote 为空或长度 < 4 | 无效输入 | 丢弃该 finding | warn `LLM_QUOTE_EMPTY` |
| quote 不在该 clause 内 | 模型改写/幻觉 | 依次下探 L2 → L3 | info `ANCHOR_FALLBACK_LEVEL` |
| 全文都不存在 | 幻觉 | 降级 CLAUSE_FALLBACK，**保留风险但标记低置信** | warn `LLM_QUOTE_NOT_FOUND` |
| 仅中文标点/全半角差异 | 归一化口径不一致 | 先做**宽松匹配**：忽略全/半角、空白、中英标点差异后再 find | info `ANCHOR_LOOSE_MATCH` |
| quote 跨两个 block | OCR 断行 / 段落切分 | 主搜索（带分隔符）失败后，在 `folded_doc_joined` 上找跨段候选，**并校验跨越的边界类型**（见下） | info `ANCHOR_CROSS_BLOCK` |
| OCR 错别字导致不完全匹配 | 扫描件 | 即裁决顺序的**第 ④ 步**（最后降级候选）：用 `difflib.SequenceMatcher` 在窗口内找相似度 ≥ 0.85 且唯一的最长子串作为候选，`anchor_score × 0.9` | warn `ANCHOR_FUZZY` |

#### 两个实现要点

**（1）宽松匹配与折叠索引映射**

归一化阶段已统一全/半角与空白，但 LLM 复述时可能又"标准化"一遍。因此搜索前对 quote 与原文窗口**同时**做一次「折叠」（去掉全部空白、标点统一为英文标点、数字统一半角），在折叠串上定位，再用预存的折叠索引映射表还原回原文坐标。**映射表必须与归一化产物一起存在于同一内存对象中**（§10.1 第 3 步已要求），不得只存折叠串。

**（2）跨 block 搜索：边界分隔符 + 边界映射表**

**不能把 blocks 直接无分隔符拼接** —— 那样会人为制造出原文中并不存在的跨段文本（block_i 的末尾 + block_{i+1} 的开头被粘成一个假 quote），使"精确唯一命中"变成假命中。

正确做法是维护**两份**折叠串，用边界分隔符区分：

```
folded_doc_sep    = block[0] + SEP + block[1] + SEP + ...     # 主搜索用，SEP = "\u0000"（1 字符）
folded_doc_joined = block[0] +      block[1] +      ...       # 仅供跨段候选使用

sep_map[k] = {                                  # 每个 block 边界一条记录
    prev_block_id, next_block_id,
    sep_offset,                                 # SEP 在 folded_doc_sep 中的位置
    boundary_kind,                              # LINE_WRAP | PARA_END
    prev_last_char, next_first_char
}
offsets[i] = (folded_start, folded_end, block_id, char_start_global)
```

- **主搜索一律在 `folded_doc_sep` 上进行**：命中区间天然不会跨越 block 边界，第 ① 步的"精确唯一命中"是真命中。
- **只有主搜索全部失败时**，才在 `folded_doc_joined` 上找跨段候选；此时必须校验它跨越的每一个边界：
  - `boundary_kind = LINE_WRAP`（上一 block 结尾无句末标点，典型如 OCR 断行）→ **允许**，确属真实连续文本；
  - `boundary_kind = PARA_END`（上一 block 以句号/分号/冒号等结尾）→ **拒绝**，判定为拼接产生的假文本。
- `boundary_kind` 在解析阶段由 `normalizer` 一次性判定，随 `sep_map` 与 `offsets` 一起持久化在同一个内存对象中。

坐标还原（命中 `[s, e)` 后）：

```
block_id          = 包含 s 的那个 block
char_start_global = blocks[block_id].char_start_global + (s - offsets[block_id].folded_start)
char_end_global   = 若 e 仍在同一 block 内，同理计算；
                    跨 block 时取结束 block 的对应偏移，区间补齐为两个 block 的完整跨度
```

**性能**：折叠全文与 `offsets` 索引在解析阶段生成一次，按 `contract_id` 缓存在进程级 LRU；单次定位复杂度为 O(出现次数 × 窗口比较)，量级远小于一次 LLM 调用，不构成瓶颈。

**可观测性**：每条 `risk_item` 都落 `anchor_method` + `anchor_score` + `occurrence_index` + `quote_context`。前端对 `anchor_score < 0.8` 或 `anchor_method = CLAUSE_FALLBACK` 的卡片显示「⚠ 定位待核对」角标。管理端统计各 `anchor_method` 占比 —— 若 `LLM_QUOTE_NOT_FOUND` 比例偏高，说明 prompt 需要加强"必须逐字复制"的约束。**这让定位质量成为可量化、可迭代的指标，而不是"感觉还行"。**

---

## 11. 风险审查流程

（编排细节见 §9.2，此处补判定规则与等级映射，直接对应需求 §2.4.4）

### 11.1 规则维度与预置规则集（seed 数据）

| 维度 | 规则示例 | 类型 | 等级 |
|---|---|---|---|
| 主体资质 SUBJECT | 相对方名称缺失 / 统一社会信用代码缺失 | MISSING | HIGH |
| 主体资质 | 相对方名称与信用代码不匹配 | REGEX | HIGH |
| 金额支付 AMOUNT_PAYMENT | **未设置付款前置验收条件**（付款条款中无「验收合格后」类表述） | EXISTS/MISSING | HIGH |
| 金额支付 | 预付款比例 > 30% | THRESHOLD | MEDIUM |
| 违约责任 LIABILITY | **我方单方承担无限责任**（含「全部损失/无限责任/不设上限」且未限定对方） | KEYWORD | HIGH |
| 违约责任 | 违约金比例超出合理范围（> 合同总额 20%） | REGEX+THRESHOLD | MEDIUM |
| 违约责任 | 双方赔偿上限不对等 | LLM | HIGH |
| 知识产权 IP | 知识产权归相对方所有（我方采购场景） | KEYWORD | HIGH |
| 争议管辖 DISPUTE | 管辖地约定为对方所在地/境外仲裁 | REGEX | HIGH |
| 保密 CONFIDENTIAL | **缺少保密义务期限** | MISSING | MEDIUM |
| 不可抗力 FORCE_MAJEURE | 未明确不可抗力通知时效 | MISSING | MEDIUM |
| 数据安全 DATA_SECURITY | 缺少数据安全/个人信息保护条款 | MISSING | MEDIUM |
| 验收 ACCEPTANCE | 缺少验收标准 | MISSING | MEDIUM |
| 条款完备性 | 缺少争议解决条款 | MISSING | HIGH |

预置 3 套规则集：`采购合同审查清单 v1`、`销售合同审查清单 v1`、`劳动合同审查清单 v1`。

### 11.2 等级与结论映射

| 综合等级 | 判定条件 | 审查结论 | 前端展示 |
|---|---|---|---|
| 高风险 | 存在任一 HIGH | `REJECT` 建议拒绝/重大整改 | 红色，卡片置顶 |
| 中风险 | 无 HIGH，存在 MEDIUM | `RECTIFY` 需整改后签署 | 橙色 |
| 低风险 | 仅 LOW 或无 | `PASS` 可通过 | 绿色 |

> **实现状态**：上表是 §11「风险评分」的**规格**，归 **Agent**，**当前尚未实现**。
> 因此 `review_task.risk_level_final` 与 `conclusion` 恒为 `NULL` ——
> Backend 侧刻意不"顺手"填一个没人负责的结论。

**人工复核（§6.3）与这套映射的关系**

人工复核**不会**触发"排除 `REJECTED` 后重算综合等级"，也**不写回**
`risk_level_final` / `conclusion`：

* 报告的风险概览按**当前 `risk_item.risk_level`** 统计，被 `REJECTED` 的风险
  **不从计数中扣除** —— 那个区域表达的是 **AI 审查发现了什么**
* `MODIFIED` 采用 Scheme A：**就地覆盖** `risk_item.risk_level`，AI 原始等级
  **不可恢复**，概览里那一行因此反映人工值
* 没有 `original_risk_level`，也没有复核历史表

（"排除误报后重算综合等级"属于 §11.2 评分的**未来**能力：要让它成立，必须先有
Agent 侧评分器 + AI 原始等级留痕，两者当前都不存在。）

---

## 12. 审批回写流程

```
法务点击「写回审批意见」
  │
  ├─ 1 组装 content_md（Jinja2 模板，对应需求 §2.4.8）
  │     ## 合同审查意见
  │     **合同名称** / **合同编号** / **送审部门** / **申请人** / **相对方** / **金额** / **履约周期**
  │     ### 一、综合审查结论
  │     总风险等级：高 | 结论：建议拒绝 | 核心风险摘要：…
  │     ### 二、风险清单明细（按等级排序）
  │     1. [高] 知识产权归属供应商 —— 页码 3 / 第 8 段
  │        原文：…
  │        依据：《民法典》第 847 条…
  │        建议修改为：…
  │     ### 三、法务批注
  │     ### 四、审查系统信息（任务号、审查时间、模型、规则集版本）← 可追溯
  │
  ├─ 2 dry_run 预览：POST /tasks/{id}/writeback {dry_run:true} → 返回 Markdown 给前端预览
  │
  ├─ 3 提交：幂等键 idempotency_key = sha256(task_id + content_hash)
  │    writeback_record: not_written → writing
  │    ApprovalClient.post_comment(instance_id, content_md, idempotency_key)
  │
  ├─ 4 结果
  │    2xx → success，记录 external_comment_id
  │    超时/4xx/5xx → failed(记录 error_msg)，waiting 用户重试
  │    幂等键重复 → 返回既有评论，视为 success（不产生第二条）
  │
  └─ 5 重试：POST /tasks/{id}/writeback/retry
       status: failed → writing；attempt+1；超过 3 次提示人工介入
       ⚠️ 重试前先查审批系统是否已有该幂等键的评论（防"写成功了但响应丢了"）
```

**"写成功了但响应超时"这个经典场景**是本设计的重点：靠**幂等键 + 回写前查重**兜底，而不是盲目重试产生重复评论。Mock 审批系统内置 `duplicate_comment` 故障注入来专门演示这个。

---

## 13. 异常和重试机制

### 13.1 错误分类（ErrorClassifier）

| 类别 | 例子 | 处理 |
|---|---|---|
| **USER_ERROR**（不可重试） | 文件加密、格式不支持、文件损坏、正文为空 | 直接 `blocked`，给出人话原因 + 修复建议，等人工换文件 |
| **DATA_ERROR**（可降级） | OCR 置信度过低、条款切分失败、LLM 返回的 quote 找不到 | 降级处理（走兜底路径）+ 记 warning，**不阻塞任务** |
| **SYSTEM_ERROR**（可重试） | LLM 超时/429/5xx、DB 瞬时连接错误、网络抖动 | 指数退避自动重试 |
| **FATAL** | 配置错误、API Key 无效、依赖缺失 | `blocked` + 告警，这是运维问题不是合同问题 |

### 13.2 三层重试

| 层 | 范围 | 策略 |
|---|---|---|
| L1 传输层 | 单次 HTTP 调用 | SDK 内置重试（Anthropic SDK 默认 `max_retries=2`，自动重试 408/409/429/5xx）+ 我们显式配置 |
| L2 阶段层 | 单个解析/审查单元（如一批条款） | 退避 `min(2^n + jitter, 60s)`，最多 3 次；单批失败不影响其它批 |
| L3 任务层 | 整个 review_task | `retry_count` 上限 3；耗尽 → `blocked` + 记录 `block_reason_code`；人工可再触发 |

**退避必须带抖动（jitter）**，避免多任务同时重试造成惊群。

### 13.3 幂等性

| 场景 | 幂等键 |
|---|---|
| 附件下载/解析 | `file.sha256`（UNIQUE） |
| 任务执行 | `sha256(contract_id + file_sha256 + rule_set_version + prompt_version)` —— 同输入同规则同 prompt 不重复审查 |
| 回写 | `sha256(task_id + content_md_hash)`（UNIQUE） |
| 写接口 | Header `Idempotency-Key` |

### 13.4 超时

| 层级 | 超时 | 超时后 |
|---|---|---|
| 单个 LLM 调用 | 120s（可配） | 该批失败 → L2 重试 |
| 单文件解析 | 300s | PARSE 失败 → L3 |
| 整个任务 | 15min | 强制 `blocked(TIMEOUT)`，释放 worker |
| HTTP 请求 | 前端 30s / 后端接口不阻塞（202） | 前端提示重试 |

### 13.5 卡死任务自愈

Worker 每 30s 心跳写 `review_task.heartbeat_at`。启动时和执行循环中扫描：`status IN (parsing, reviewing) AND heartbeat_at < now() - 5min` ⇒ 判定 worker 崩溃，任务回退 `pending` 重新入队（`retry_count+1`）。这是"进程被 kill 也不会永久卡住"的保障。

### 13.6 日志

- 结构化 JSON 日志（`request_id` / `task_id` / `contract_id` / `stage` 贯穿全链路）；
- `task_event` 表 = 给前端展示的任务时间线（什么时候进什么状态、什么原因）；
- `ai_call_log` 表 = 每次模型调用全留痕（token、耗时、原始响应、prompt 版本）。

---

## 14. Mock 审批系统设计

### 14.1 定位

真实企业审批系统（如钉钉/飞书/泛微/OA）我们接不到，所以**在本系统内实现一个 Mock 审批模块**，它扮演"外部系统"的角色：

- 有**独立的数据表**（`approval_instance` / `approval_comment`），不与合同表混用；
- 通过 **`ApprovalClient` 抽象接口**访问，与未来接真实系统同签名；
- 有**独立的展示页面**（`MockApprovalView.vue`），可以像外部系统一样被操作。

> 架构意义：Mock 不是"假数据填充"，而是**防腐层（Anti-Corruption Layer）**。面试时可以讲："我们把外部依赖抽象成接口并用 Mock 实现，真实对接时只替换适配器，业务代码零改动。"

### 14.2 模拟的三个能力

| 能力 | 接口 | 说明 |
|---|---|---|
| 待办拉取 | `GET /mock-approval/instances?cursor=&limit=` | 支持游标增量，对应真实系统的分页待办列表 |
| 附件下载 | `GET /mock-approval/instances/{id}/attachment` | 返回本地 `samples/` 里的示例合同文件 |
| 评论写回 | `POST /mock-approval/instances/{id}/comments` | 带 `Idempotency-Key`；重复键返回既有评论不新建 |

### 14.3 故障注入（★ 演示阻塞重试与幂等的关键）

`POST /mock-approval/fault-config`：

| 开关 | 作用 | 演示什么 |
|---|---|---|
| `latency_ms` | 人为延迟 | 超时处理 |
| `fail_rate` | 按概率返回 500 | 任务阻塞 + 自动重试耗尽 → `blocked` |
| `fail_times` | 前 N 次失败第 N+1 次成功 | 重试成功的完整链路 |
| `duplicate_comment` | 写回时先成功但返回超时 | **幂等性验证**（不产生重复评论） |
| `reject_encrypted_file` | 返回一个加密 PDF | `FILE_ENCRYPTED` 阻塞 + 人工重新上传 |

有了这个开关，**"异常任务阻塞与重试流程"（需求 §2.4.7）可以在演示时一键复现**，而不是靠嘴说。

### 14.4 预置数据

`scripts/seed_mock_approval.py` 灌入 4 张审批单：

1. 软件采购合同（知识产权归属供应商 + 到货即付全款无验收）→ **端到端主演示场景**
2. 大额销售合同（我方无限责任 vs 对方每日万分之一）→ 违约责任不对等场景
3. 劳动合同（保密期限缺失 + 竞业限制无补偿）→ 中风险场景
4. 扫描件采购合同（图片 PDF）→ OCR 链路场景

---

## 15. 第一版 MVP 范围

**目标：一个能真实跑通「上传→解析→审查→复核→回写→导出」的完整闭环**，而非 UI Demo。

> ⚠️ **归属列是本节最重要的信息。** 同一个"模块"，写在不同服务里是完全不同的工作。
> 判断依据见 §1.1：**智能归 Agent，数据与基础能力归 Backend。**

### ✅ 必做（MVP）

| 模块 | 归属 | 范围 |
|---|---|---|
| 合同接入 | **Backend** ✅已实现 | 本地上传 + 扩展名/MIME/魔数三层校验 + sha256 去重 + 两层幂等 + Mock 审批待办拉取 |
| 编排骨架 | **Agent** ✅已实现 | LangGraph 工作流 + State + Conditional Edge + HTTP 编排入口 |
| 文档解析 | **Agent** | DOCX 段落+表格；PDF 文本层；扫描件 OCR（RapidOCR）；混合 PDF 按页处理 |
| 归一化与定位 | **Agent** | 全局字符偏移坐标系 + 页码 + 段落索引（DOCX 页码可空） |
| 元数据提取 | **Agent** | 正则规则 6 个字段 + LLM 兜底；带定位 |
| 条款切分 | **Agent** | 规则切分（编号模式）+ LLM 兜底；11 类条款类型 |
| 规则**求值引擎** | **Agent** | 5 种求值器：KEYWORD / REGEX / EXISTS / MISSING / THRESHOLD |
| 规则**数据与 CRUD** | **Backend** | 3 套预置规则集（合计 ~14 条规则）+ 规则管理接口 |
| LLM 审查 | **Agent** | Provider 抽象 + DeepSeek 实现；条款分批并发审查；结构化 JSON + 四道防线 |
| 风险评分 | **Agent** | 加权评分 + 高风险一票定级 + 结论映射 + 人工覆盖 |
| 建议生成 | **Agent** | LLM 内联建议 + 标准条款库匹配 |
| 结果持久化 | **Backend** | block / clause / metadata / risk / suggestion 的落库接口 |
| 前端 | **Frontend** | 大盘、列表、工作台（双栏 + 高亮联动）、规则配置、标准条款库、Mock 审批页 |
| 文档预览 | **Frontend** | pdfjs-dist（PDF）+ docx-preview（DOCX）+ 双向高亮 |
| 审批回写 | **Backend** | Markdown 生成 + 幂等写回 + 失败重试 + 状态轮询 |
| 报告导出 | **Backend** | 基于**已持久化**的审查结果**实时生成** Markdown（只读投影）；不重跑 LLM / 规则 / 合并 / 解析，不建报告表、无额外依赖。P12 MVP 只有 Markdown |
| 可靠性 | **Backend** | 状态机 + task_event + ai_call_log + 三层重试 + 心跳自愈 + 故障注入开关 |
| 演示 | —— | 场景 1（软件采购合同）端到端全流程 + 场景 2/3 作为数据补充 |

### ⏸ 明确不做（第一版）

- **认证与鉴权（JWT / RBAC / 登录）** —— 单机演示不需要，见 §17.6
- **人工审核进 LangGraph** —— 不引入 `interrupt()` / checkpoint，见 §1.1
- 真实审批系统对接（钉钉/飞书/OA）—— 只留 `HttpApprovalClient` 空实现 + 接口文档
- 多租户、组织架构同步、SSO
- 合同版本比对（新旧两版条款 diff）
- 规则可视化拖拽编辑器（第一版用表单 + JSON 表达式）
- 模型微调 / 向量检索 RAG 标准条款（第一版用 `clause_type + tags` 精确匹配）
- 多语言合同（只做中文）
- 词级 OCR 坐标精确框选（第一版按段落级高亮，bbox 已存但前端只做段落级）

---

## 16. 后续增强功能

| 优先级 | 功能 | 价值 |
|---|---|---|
| P0 | 接真实审批系统（钉钉/飞书/OA 开放平台） | 落地必需 |
| P0 | 合同版本比对（新旧版本条款 diff + 风险变化） | 法务真实高频需求 |
| P1 | 标准条款库向量检索（RAG），提升建议匹配质量 | 建议采纳率提升 |
| P1 | 规则可视化编排（拖拽条件组） | 让业务人员自助维护规则 |
| P1 | 审查结果反馈闭环：法务的采纳/驳回回流为评估集，定期跑 prompt 回归 | 效果可持续优化 |
| P2 | 多模型对照审查（DeepSeek vs Claude 双跑，差异高亮） | 提升准确率与可信度 |
| P2 | 合同要素抽取升级为表格结构（当事人表、付款计划表、里程碑表） | 结构化程度更高 |
| P2 | 异步导出队列 + 大报告分片渲染 | 大合同性能 |
| P3 | 权限细化到字段级、操作审计报表 | 合规要求 |
| P3 | 移动端适配 | 审批场景随时处理 |

---

## 17. 项目实施阶段划分

> **本文档是阶段划分的唯一基准。** 与代码实现不一致时，改文档或改代码，**不允许两个版本并存**。
> 每阶段遵循：**先设计 → 再编码 → 再验证**，验证通过才进下一阶段。

### 17.0 为什么旧路线作废（架构调整说明）

旧版本文档的路线 `P2 → P3 → P4 → P5 → P6 → P7 → P8 → P9 → P10 → P11` 描述的是
**单体 Backend 承担全部职责**：解析、条款、元数据、规则、LLM、编排全部写在 `backend/app/` 里。

**这个前提已经改变。** 当前实际架构是：

```
Frontend  ──►  AI Agent Service（FastAPI + LangGraph）  ──►  Backend Service  ──►  MySQL
               ★ 全部智能能力                              ★ 数据与基础能力
```

因此旧路线中"属于 Backend 的智能职责"**已整体改归 Agent**。

* **旧编号 P5 / P6 / P7 / P8 / P9 / P10 / P11 及其子编号 P4b / P7b / P8b / P10b / P11b 一律作废**，不再使用
* 已完成的历史阶段 **P1 ~ P4 保留原编号**（它们的工作内容没有变，只是归属说明需要修正）
* Agent 相关工作沿用已有的 **P5 系列子编号**（P5-1 ~ P5-4 已完成），后续阶段从 **P6** 继续

### 17.1 已完成阶段

| 阶段 | 内容 | 状态 | 验证 |
|---|---|---|---|
| **P1 架构设计** | 本设计文档 | ✅ | 已确认（本文档正在做架构同步） |
| **P2 技术架构与环境基座** | 依赖与版本锁定、`.env` 规范、uv workspace、日志/配置/异常基座、MySQL 建库、Alembic 初始化、前后端可各自启动 | ✅ | 后端 `uvicorn` → `/docs`、`/health`；前端 `npm run dev` |
| **P3 数据库设计落地** | 15 张表模型 + baseline 迁移 + 规则 seed | ✅ | `alembic upgrade head` / `check` / `downgrade base` 全通过 |
| **P4 合同接入与文件生命周期** | StorageBackend 抽象、SHA-256 流式去重、扩展名/MIME/魔数三层校验、上传 API、Contract/ContractFile/ReviewTask 创建、并发补偿清理、**两层幂等** | ✅ | 342 passed；真实端到端上传 + 重复上传复用 |

### 17.2 P5 —— AI Agent 基线（进行中）

**目标**：让 Agent 成为一个**可被真实调用的独立服务**，跑通最小 LangGraph 骨架。

| 子阶段 | 内容 | 状态 |
|---|---|---|
| **P5-1** | Preflight：确认 LangGraph 版本与 API、环境约束 | ✅ |
| **P5-2** | Agent 服务外壳：FastAPI + `AgentSettings` + 目录骨架 + `GET /health` | ✅ |
| **P5-3** | 最小 LangGraph：`ContractReviewState` + `upload_file` / `validate_file` / `parse_document` + Conditional Edge | ✅ |
| **P5-4** | Agent HTTP 编排入口 `POST /api/agent/review` + BackendClient 生命周期 | ✅ |
| **P5-5** | Agent Tool 契约固化：把 Backend 调用整理成显式 Tool 层，补齐 Agent↔Backend 的接口契约文档 | ⏳ 下一步 |

**P5 的整体验收标准**

1. Agent 是**独立 FastAPI 服务**（`127.0.0.1:8001`），`GET /health` 可用
2. `POST /api/agent/review` 上传一份文件后，能真实走完
   `upload_file → validate_file →[条件边]→ parse_document` 并返回结果
3. **端到端成立**：Frontend 请求 Agent → Agent 调 Backend → Backend 写入 MySQL → Agent 返回 State 投影
4. Conditional Edge 真的在分流：非法文件停在 `validate_file`，不进入解析
5. Agent 侧 `pyproject.toml` **不含** SQLAlchemy / aiomysql
6. 两个服务的测试与 ruff 全绿

> ⚠️ **`parse_document` 在 P5 阶段保持 `StubParser`（桩实现）是刻意保留的**，不是未完成。
> 真实 Parser 属于 P6，且**第一版只做 DOCX** —— 不要因为架构调整而提前把 PDF / OCR 加进来。

### 17.3 黄金链路（后续阶段，串行）

**推进原则：先把一条最细的端到端链路跑通并固化，再横向扩展能力。**
禁止出现"OCR 都做完了但 DOCX 主链路还没通""Agent 全做完了但前端还看不到"这类横向铺开。

```
★ 黄金链路（Golden Path）—— 架构调整后的版本

  Frontend 上传 DOCX
      → Agent ① upload_file → ② validate_file → ③ parse_document(DOCX)
      → ④ identify_clauses → ⑤ extract_metadata → ⑥ extract_keywords
      → ⑦ rule_review → ⑧ llm_review → ⑨ merge_risks
      → ⑩ generate_suggestions → ⑪ persist_result（经 Backend API 落库）
          → Frontend 工作台展示（合同列表 → 工作台 → 风险卡片 → 原文高亮定位）

  范围界定（刻意收窄）：
    文件格式：只支持 DOCX。PDF / 扫描件 / 图片 一律不接（见 §17.5 扩展阶段）。
    定位精度：block/clause 级 + 段落索引；DOCX 的 page_number 恒为 NULL（§10.4）。
    规则集：只启用「采购合同审查清单 v1」中的 3~5 条启发性规则。
    LLM：单一 Provider（DeepSeek），条款串行调用，不分批并发。
    前端：只做 合同列表页 + 工作台双栏 + 风险卡片。大盘/规则配置/模板库全部后置。
    执行方式：Agent 在**一次 HTTP 请求内同步跑完**整张图，不引入队列与 Worker。
    人工审核：**不在图内**。工作台先只做展示，确认/驳回/修改属扩展阶段（P13）。
    明确不做：报告导出、审批回写、批注、元数据人工校正。

  最小核心幂等（黄金链路唯一保留的幂等能力）：
    ① 文件级：contract_file.sha256 UNIQUE                        ✅ 已实现
       —— 同一文件重复上传不重复解析、不重复计费
    ② 任务级：review_task.idempotency_key UNIQUE                  ✅ 已实现
       —— sha256(contract_id + file_sha256 + rule_set_version + prompt_version)
          同配置复用同一任务；换配置（新规则集/新 prompt）才新建任务
    ③ 风险级：risk_item 落库前按 (task_id, clause_id, dimension, 归一化 risk_title) 去重
       —— 防止重试或分批导致同一风险重复入库（对应 §9.2 步骤 2c 的合并键）  ⏳ P9
```

**黄金链路验收标准（一句话）**：把 `samples/采购合同-风险版.docx` 上传后，**5 分钟内**在浏览器工作台看到
「知识产权归属供应商」「未设置付款前置验收条件」两条高风险卡片，点击卡片能跳转并高亮到对应段落。

### 17.4 阶段明细

| 阶段 | 目标 | 修改范围 | 技术学习点 | 验收标准 | 前置 |
|---|---|---|---|---|---|
| **P6**<br>真实文档解析 | 把 `StubParser` 换成真实解析，产出 `document_block` | **仅 `ai-agent/`**：`app/parsers/`（`DocumentParser` 抽象 + `DocxParser`）、重写 `graph/nodes/parse_document.py`、Backend 持久化 API | 抽象基类与工厂、`python-docx` 的 body 元素顺序遍历、归一化坐标系与索引映射、阻塞 IO 走线程池 | DOCX 解析出 blocks；全局偏移能正确切出原文；加密/损坏文件走 blocked | P5 |
| **P7**<br>理解层 | 条款识别 + 元数据提取 + 关键词提取 | **仅 `ai-agent/`**：`app/understanding/`、三个对应节点 | 规则优先 + LLM 兜底的降级设计；结构化输出契约设计；"LLM 只给 quote，坐标自己反查" | 示例合同切出的条款数与人工核对一致；元数据提到主体/金额/期限 | P6 |
| **P8**<br>规则审查 | 规则求值引擎：加载规则 → 逐条评估 → 输出命中 | **仅 `ai-agent/`**：`app/rules/`、`rule_review` 节点；规则数据经 Backend API 读 | 求值器模式；规则与 LLM 的分工（确定性 vs 语义）；**规则数据归 Backend、求值引擎归 Agent** 的切分 | 示例合同命中 3~5 条预置规则，与人工核对一致 | P7 |
| **P9**<br>LLM 审查 + 风险合并 | DeepSeek 结构化审查 + 规则/LLM 结果合并去重 + 评分 + 修改建议 | **仅 `ai-agent/`**：`app/llm/`（Provider + Prompt 模板 + json 守卫）、`llm_review` / `merge_risks` / `generate_suggestions` 节点 | `ChatPromptTemplate`、structured output、Pydantic 强校验、四道防线、风险合并键设计 | 场景合同跑出预期的两条高风险与对应建议 | P8 |
| **P10**<br>结果持久化 | 把 Agent 的产出落进 Backend 的库 | **Backend**：document/risk/suggestion 的持久化 API；**Agent**：`persist_result` 节点 | 服务间写入的幂等设计；Agent 不碰 ORM 时如何保证事务边界 | 跑完一次审查后，库里出现对应的 block / clause / risk_item / risk_suggestion | P9 |
| **P11**<br>前端工作台 | 合同列表 + 双栏工作台 + 风险卡片 + 原文高亮 + 建议展示 | **仅 `frontend/`**（Backend 补合同查询 API） | 风险↔原文双向高亮联动；大文本渲染性能；轮询与退避 | 点风险卡片 → 跳转并高亮到对应段落 | P10 |
| **P12**<br>报告生成与导出 | 审查报告（**Markdown**），可下载 | **仅 `backend/`**：`services/report_query.py`（只读查询）+ `services/report_render.py`（纯函数渲染）+ `api/v1/endpoints/reports.py`（下载）。**Agent 不参与** | 只读投影与数据隔离（任务级 `task_id` vs 文件级 `file_id`）；渲染的确定性与无副作用；中文文件名 RFC 5987；**报告只做事实陈述、不产生审查结论** | 导出报告可打开，内容与工作台一致（有交叉一致性测试守着） | P11 |
| **P13**<br>Human Review | 人工确认 / 驳回 / 修改风险，并留痕 | **Backend**：Human Review API + `risk_item.review_status` 流转；**Frontend**：复核交互 | 人工状态与自动状态解耦（两个维度不混用）；**为什么不用 `interrupt()` 做人工审核** | 确认/驳回/修改后状态与留痕正确；重跑审查不覆盖人工结论 | P11 |
| **P14**<br>异步执行 | 当同步编排撑不住时，引入任务状态机 + DB 队列 + Worker | **Backend**：`workers/`、状态机 + `task_event`；Agent 侧接口不变 | DB 当队列（`FOR UPDATE SKIP LOCKED`）、锁不跨长任务、心跳自愈、三层重试 + 错误分类 | 建任务后 worker 抢占并推进状态；kill worker 后任务能自愈回 `pending` | P10 |
| **P15**<br>审批回写 | 把审查意见回写到（Mock）审批系统评论区 | **Backend**：`integrations/ApprovalClient`（Mock / Real 同接口）+ 回写幂等 | 防腐层设计；"写成功了但响应超时"导致的重复评论怎么防 | 回写后 Mock 审批页看到评论；重复提交不产生第二条 | P13 |
| **P16**<br>异常演练 + 联调 | 故障注入、阻塞处理、全链路演示 | 全栈 | 把超时/失败/重复回写做成可配置开关 | 注入 100% 失败 → 任务 blocked；关闭后重试成功 | P14 |
| **P17**<br>测试与文档 | 补齐单测/集成测试、README、接口文档 | 全栈 | —— | `pytest` 全绿；关键模块覆盖率 > 70% | P16 |

### 17.5 阶段依赖

```
黄金链路（串行，必须按序，每步都要能端到端跑通）：

  P1 ✅ → P2 ✅ → P3 ✅ → P4 ✅
                            │
                            ▼
                  P5 (P5-1 ✅ P5-2 ✅ P5-3 ✅ P5-4 ✅ P5-5 ⏳)
                            │
                            ▼
        P6(解析·仅DOCX) → P7(条款/元数据/关键词) → P8(规则审查)
                            → P9(LLM审查+风险合并+建议) → P10(结果持久化)
                            → P11(前端工作台)
                                       │
                        ★ 黄金链路验收点 ┘
                                       │
扩展阶段（此后可并行推进，顺序可调）：    ▼
        P12(报告)  ─┐
        P13(人工复核)├─→ P15(审批回写) → P16(异常演练+联调) → P17(测试与文档)
        P14(异步执行)┘
```

**并行提示**：P12 / P13 / P14 三者互不阻塞，可任选顺序。

### 17.6 本轮明确不做（避免为了"显高级"引入复杂度）

| 不做 | 理由 |
|---|---|
| **JWT / RBAC / 登录** | 本项目是学习与面试项目，单机演示不需要鉴权。旧文档的 P4b **作废** |
| **Redis / Celery / Kafka / 消息队列** | P14 的异步执行用 **MySQL 表当队列**即可，这一条本身就是可讲的面试点；引入中间件只会稀释重点 |
| **K8s / 微服务拆分 / 服务注册发现** | 两个进程而已。Agent 与 Backend 已经是两个服务，边界问题已经讲清楚了 |
| **`interrupt()` / checkpoint / resume 实现人工审核** | 人工审核是普通状态流转，用 `risk_item.review_status` 就能表达（见 §1.1） |
| **PDF / OCR** | 属扩展阶段。黄金链路只做 DOCX（§10 的算法设计保留，实现后置） |
| **Agent 直连数据库** | 架构红线。Agent 的依赖声明里刻意不含 SQLAlchemy / aiomysql |

**阶段验收纪律**：每个阶段结束时必须能跑通"当前已完成全部能力"的端到端路径，并把验证命令写进该阶段文档。**不允许出现"这个阶段做完了但跑不起来，等下个阶段补"。**

---

## 附：关键设计点的面试话术（你在原则 20 里要求的）

| 设计点 | 一句话话术 |
|---|---|
| 为什么规则 + LLM 混合 | "硬性红线用规则保证确定性和可审计，语义研判交给 LLM 保证覆盖面；只用规则会漏语义风险，只用 LLM 会漏硬规则且不可解释、成本高。" |
| 统一坐标系 | "全系统只有一套全局字符偏移坐标系，解析时生成，风险/元数据/批注/建议全部复用，从根上避免多套坐标对不上导致的高亮错位。" |
| LLM 不许自报坐标 | "模型只输出 quote，坐标由 Agent 反查。模型在长文本里数偏移量必然幻觉，把定位权收回到确定性代码里。" |
| 两阶段状态机不可回退 | "审查结论是审计证据，completed 后不可变；重新审查新建任务，历史全留存。" |
| 幂等回写 | "回写的幂等键是 task_id + 内容哈希。最危险的不是失败，是'写成功了但响应超时'导致重复评论——所以重试前先按幂等键查重。" |
| 三层重试 + 错误分类 | "先分类再重试：用户错误直接阻塞等人工，系统错误才退避重试，数据质量问题走降级不阻塞任务。盲目重试只会放大故障。" |
| Worker 心跳自愈 | "任务表带 heartbeat_at，扫描超时任务回退 pending 重新入队，进程被 kill 也不会留下永久卡死的任务。" |
| Mock 审批系统的定位 | "它不是假数据，是防腐层。外部依赖抽象成接口 + Mock 实现，真实对接只换适配器。" |
| 故障注入 | "把超时、失败、重复写回做成可配置开关，异常流程可以一键复现演示，而不是靠截图讲故事。" |
| LLM 结构化输出四道防线 | "协议层约束 + 提示层说明 + 解析层修复重试 + 校验层强校验；最后仍失败就降级为仅规则结果并标记告警，绝不让一个批次拖垮整个任务。" |
| 附件去重 | "文件 sha256 唯一索引，同一份合同重复提交不会重复解析、重复审查、重复计费。" |
| **并发模型边界** | "阻塞调用会卡死整个 event loop。我按'原生 async / to_thread / 独立进程池'三档划分：LLM 和 DB 是 async，解析库是 C 扩展所以走线程池，OCR 这种秒级 CPU 任务走独立进程池——因为 onnxruntime 长时间占 GIL，扔线程池照样拖慢事件循环。心跳也做成独立后台任务，保证 OCR 卡住时不会误判任务死亡。" |
| **抢占事务边界** | "锁只覆盖抢任务那一条 UPDATE。`FOR UPDATE SKIP LOCKED` 抢到行后立刻改状态、写事件、commit，锁就释放了，OCR 和 LLM 全程在事务外跑。用长事务包住长耗时任务会拖垮连接池并产生大量锁等待——这是 DB 当队列时最容易踩的坑。" |
| **quote 消歧定位** | "LLM 只给 quote 不给坐标，后端按'条款内 → 条款的 block 区间 → 全文'四级作用域反查；同一作用域出现多次时用前后文相似度打分消歧，分差不够就降级到条款级并提示人工核对。原则是宁可降级不可错标——法务最不能容忍的是风险卡片指错位置。" |
| **DOCX 页码语义** | "python-docx 拿不到真实页码，因为页码是排版结果不是文档属性。所以第一版 DOCX 的 page_number 恒为 NULL，用段落索引 + locator_type 字段显式区分定位方式，前端不显示任何'第 N 页'文案。宁可承认限制，也不能给法务一个翻不到的假页码。" |
| **黄金链路** | "我先把一条最细的端到端链路跑通——上传 DOCX 到前端看到高风险卡片并高亮定位，然后再横向扩 PDF、OCR、回写、报告。避免出现每个模块都做了一半、但整条链路跑不起来的常见失败模式。" |
| **为什么拆出 AI Agent 服务** | "智能能力和数据能力的生命周期完全不同：前者要反复换模型、调 Prompt、调解析策略，后者要保证事务、约束和幂等。放在一个进程里，换一次 Prompt 就可能碰到 ORM 的边界。拆开之后，Agent 的依赖声明里干脆没有 SQLAlchemy——边界不是靠自觉，是靠依赖关系强制住的。" |
| **Agent 怎么访问数据** | "Agent 只通过 HTTP 调 Backend 的领域 API。它自己算出来的 sha256 不算数，必须由 Backend 去查重、去判幂等——校验规则只有一份，否则两边一定会漂移。" |
| **人工审核为什么不用 interrupt** | "人工审核的等待时间以小时计。用 `interrupt()` 就得长期持有一个可恢复的执行上下文和 checkpoint 存储，而它本质上只是一个字段的状态流转。把它留在图里是把简单问题复杂化。" |

---

**本文档（v2.0）为架构同步后的生效版本，是后续开发阶段划分与职责边界的唯一基准。**
**当前进度：P1 ~ P4 已完成，P5（AI Agent 基线）进行中，下一步为 P5-5。**

**阶段规则：** 每阶段先设计 → 再编码 → 再验证 → 汇报 → 停止；验证通过才进入下一阶段。
每阶段结束时必须能跑通"当前已完成全部能力"的端到端路径 —— **不允许出现"这个阶段做完了但跑不起来，等下个阶段补"。**
