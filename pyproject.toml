[project]
name = "contract-approval"
version = "0.1.0"
description = "企业合同审批审查系统"
requires-python = ">=3.12"
dependencies = []

# 本文件是 uv workspace 根，本身不承载业务代码，也不作为 Python 包安装。
# 后端真实依赖声明在 backend/pyproject.toml（见架构文档 §2.1）。
[tool.uv]
package = false

[tool.uv.workspace]
members = ["backend"]
