# 开发环境变量（架构文档 §2.2）
#
# 业务接口基础路径。开发期由 vite.config.ts 的 server.proxy 转发到后端
# http://127.0.0.1:8000，因此这里用相对路径即可，无需处理跨域。
VITE_API_BASE_URL=/api/v1

# Agent 服务的接口前缀（P14-5-1）。
#
# ⚠️ 它与上面那条**不是**同一个服务：Backend 是数据层（:8000），Agent 是审查
# 编排服务（:8001）。前端唯一要打的 Agent 接口是 POST /api/agent/review，
# 由 vite.config.ts 的 /api/agent 代理转发 —— 注意那条规则必须排在 /api 前面。
VITE_AGENT_BASE_URL=/api/agent
